# Changelog (simple)

[2026-08-20 22:05:45 UTC] [Docs/README] Motivation paragraph opening "How this relates to Claude Code's own coordination": the in-task shapes kept failing the same way (a teammate reporting *finished* without delivering its report, leaving reconstruct-from-memory as the only recovery), against a peer that owns a terminal and a file on disk and so fails visibly. 5 lines above the existing intro paragraph, table and all following prose untouched. `b3869a5`, straight onto main.

[2026-08-18 23:56:54 UTC] [Docs/Cross-session] `cbus-kda` the closed-boundary rationale is retired: Claude Code shipped cross-session messaging (2.1.224+, measured on 2.1.235), so "there is no cross-session addressing at all" -- the sentence four docs built the project's reason to exist on -- is false. README gains a "How this relates to Claude Code's own coordination" section with a fact-checked 8-row table over Subagent / Agent Teams / SendMessage / Workflow / cbus, plus the honest durability limit (a send to an armed-then-dead listener is refused without `--force`; forced mail replays on re-arm but not across a re-join, since join truncates; the relay spools outside the ~90-120s silent-WS-drop window already in CHEATSHEET.md:222-226). docs/how-it-works.md and docs/architecture/overview.md (3 sites) mark the vertical/horizontal split superseded and keep the original probe as a dated block rather than deleting it; the "Monitor-tail is the only turn-native answer" claim is qualified now that hooks and Bash children are handed `CLAUDE_CODE_MESSAGING_SOCKET`/`_TOKEN` (verified in this session's own Bash env). docs/prior-art-and-cc-internals.md gets a new section 6 with the measured findings, tagged [M] measured / [D] doc-derived / [1] single-source. Findings came from this session plus two independent peer sessions on the bus, one briefed to refute rather than confirm; that peer caught a column misalignment, a misquote presented as exact, and the false "nothing outside a model turn" claim. Docs only, no behavior change; full suite green, `go build ./...` clean. Branched off main in a worktree; nothing pushed.

[2026-08-13 20:15:00 UTC] [Roles/Commands] `6920262` opus model token pinned to the full id `claude-opus-4-8` (temporary; Carlos's ruling from live formations: the F5-orchestrator + O4.8-coder pairing beats O5). Bare `opus` now resolves to Opus 5 in the harness, so every seat that said "opus" had silently changed generation. The full id passes the `^[A-Za-z0-9._-]+$` name screen and Spawn's pre-fork gate unchanged, so no code moves: `MODEL:` lines in roles/coder.md + roles/orchestrator.md, the model-values instruction in commands/bus-spawn.md + bus-branch.md, and the role_test literal that guards the ruled value (expectations are literals; it moves with the ruling). Live stores were pinned by hand on both machines since only a release refreshes them: $CBUS_DIR/roles (coder/orchestrator/tester -- tester is runtime-only, no repo copy), the installed /bus-* skills, and every saved formation envelope with an explicit `"model": "opus"` (33 fields MBP, 3 NUC; explicit beats the role default, so both layers were needed; all JSONs re-parsed clean). The next selfupdate/install-roles from a release WITHOUT this commit restores `opus` in the installed roles+skills; the .formations edits persist (user data). The parked orch-fable re-flip (25189d1) edits the same orchestrator MODEL line; fable wins that seat when it lands. Suite green after the literal move; the doctrine canary passes (MODEL header sits outside the shared block).

[2026-08-09 23:10:00 UTC] [Formation/Resume] `7903eb1` Pre-profile-capture envelopes record no profile, so a bare shell (or the dashboard button, same environment) resolved no instance roots and read every profiled transcript as gone -- 16 of 17 envelopes in the field store were button-unresumable, found by the first operator test on an older formation (android-waiver-scan: "aged out" while the transcript sat 1.6MB under the work instance). The resume transcript gate now sweeps ~/.ccs/instances/*/projects for the sid when the recorded profile is blank; a unique owner is adopted for BOTH the gate and the launch prefix (find and launch can never disagree), reported to the operator, and stamped by the anchor's next save. Multiple owners refuse by name, swept-empty refuses stating the search was exhaustive, a recorded profile is never overridden, and TranscriptPath stays narrow (the sweep is a named verb-level recovery, not an implicit widening). Closes cbus-kl4.

[2026-08-09 21:55:00 UTC] [Transcript/Fix] `fb948f8` Bare shells have no CLAUDE_CONFIG_DIR, and transcriptRoots gated the CCS profile root on it — so every bare-shell or GUI-launched resume of a PROFILED formation refused on a transcript that was present the whole time (the post-reboot terminal and the new ccresume:// formation button are exactly such shells; every prior field test masked it by running from an env-carrying session shell). The profile root now resolves from HOME + the recorded profile, the same trust move anchorLaunchPrefix already made; cfg-derived roots kept for non-standard layouts. Found by the ccresume handler field test refusing a warm launchtest formation; regression test pins the bare-shell lookup and its blank-profile inverse. Ships as v0.9.1.

[2026-08-08 20:15:00 UTC] [Formation/AnchorModel] `b78f1bc` + `9bfd332` (+ three test riders) The anchor model lands: an envelope always has an anchor, and the restored anchor decides. j4i (`b78f1bc`): save REFUSES to mint an anchorless envelope, naming both remedies (join and re-save, or set anchorAlias by hand) with nothing written -- refuse-over-autopick, since a wrong auto-anchor becomes the wrong restore seat later. A refresh of a legacy anchorless file still saves and WARNS (refusing would strand the files this feature exists to converge -- a Validate hard error would kill the heal through the refuse-to-overwrite load, which is why the warning lives in AnchorWarning, not Validate); show renders the defect row; joined re-saves heal via anchorDefault so the fleet converges with no migration. zhj (`9bfd332`): anchorKickoff is now a decision brief -- the roster rendered into the restored anchor's first turn (per peer: alias, saved mode, origin, transcript present/GONE/unchecked-on-another-machine/none-recorded, machine), the anchor marked as the deciding seat with nothing to decide about itself, per-peer resume/recreate/skip asked, operator confirmation, then the ruling expressed in apply flags with REAL aliases -- and the examples are runnable-as-written: only present-transcript, on-this-host peers get named (blank machine means here, mirroring the apply gate exactly), capped at two, the line absent when nobody is resumable. Liveness is deliberately NOT in the roster: every row field is stable at compose time, and the brief hands the volatile fact to the dry-run by name ("who is present RIGHT NOW"). Closes with the reconvene re-save instruction. Riders `2d262c2`/`324af80`/`76cc0a2` cover the save-door warning, the example contract both directions, and the SidState mirror with the cross-machine-but-visible discriminating input. Both beads reviewer-approved across sixteen pre-registered gates with reviewer-run mutants throughout.

[2026-08-08 07:35:00 UTC] [Formation/ModeGuard] `1d4488f` + `f4bb204`..`a780785` The late-bound resume controls, built by a formation using the machinery it improves. --mode (`1d4488f`): `formation apply --mode resume|fork|template`, a per-run in-memory override on the overrideChannel precedent -- the envelope is never written, present peers are untouched by ORDERING (decidePeer returns present/skip before it reads mode), every identity gate fires under the override so a blanket resume degrades or refuses per peer, and it composes with --only for per-peer steering. The answer to "how do I flip one peer to resume" stops being a jq one-liner. Launch-intent guard (`f4bb204` + two ruled fixes + `a780785`): closes the fork-to-rejoin window where liveSids is blind and a second resume would double-launch one transcript. Channel-dir marker (the peer dir was falsified as a location: Join RemoveAll would hand the guard to the party it guards against), first-writer-wins os.Link claim (temp+rename was falsified by a 16-racer probe: last-writer-wins let 2-4 of 16 fork), corpse OVERWRITTEN never unlinked (absence is the signal racers wait for, so any reclaim that unlinks first reopens the race it heals -- this falsified the fix sketch itself), flock on the reclaim window only (third race of the same class in one milestone = the mec.2 kernel-primitive heuristic, and acquireMintLock had already written the lesson down), cleared by same-sid Join or TTL 180s. Marker and reclaim window want OPPOSITE lifetimes -- one must survive its writer, one must die with its holder -- which is why one is a file and one is a kernel lock. Reviewer-gated end to end: two blocking findings (F1 the claim race, F2 two mutators outside the flock), both fixed and re-walked; six mutants incl. one reverting the marker to the falsified location and one proving the SIGKILL test measures kernel-release specifically. `dede3f5` is a pre-existing gofmt fix, neither bead.

[2026-08-07 21:35:00 UTC] [Formation/Resume] `8e30eac`+`409ab7d` Workstream B of the northstar reset, the first hop after a reboot. yv3: join stamps the session's CCS profile into peer meta (derived structurally from CLAUDE_CONFIG_DIR, blank outside CCS; omitempty so old metas rewrite byte-identically) and formation save captures it like cwd -- refresh on non-blank meta, blank never clobbers a hand fill, garbage tokens skipped and surfaced. lsy: new verb `cbus formation resume <name> [--brief]` relaunches the ANCHOR session from a bare shell -- right cwd, right profile (`ccs <profile>` even when the shell has no CCS env, which is exactly the post-reboot case), `--resume` its own sid, kickoff briefing it to re-join, re-arm, and reconcile the fleet via apply. Refuses loudly instead of degrading: no anchor, wrong machine, no/dup sid, fork-born or unattributed origin, live-armed sid, gone transcript -- each names its remedy, and a refusal launches nothing (pinned). First real-store smoke caught a gate-order defect before commit: a live-armed anchor whose transcript hides under another profile drew the transcript refusal instead of the decisive "already running, go use it" -- live-sid now checks first, pinned by a discriminating test where both gates are true. Suites green both packages, linux amd64+arm64 gate PASS.

[2026-08-05 02:45:05 UTC] [Formation/Save] `c1ef5fa` Repeatable `--anchor key=value` on `formation save`: writes hand anchors into `drift_anchors`, where the preservation rule already keeps them across re-saves -- the flag is sugar over what hand-editing the envelope could do, minus the editor. An explicit flag overwrites its own key (the preservation rule protects hand edits from the MACHINE; a flag is the hand acting); `git_head` is refused up front as the one machine-owned key, before any store work, so a refused save leaves no envelope behind. Convention the bd-dashboard formations page reads: `--anchor bdx=<epic-id>` links the effort's tracker item and wins over session-join inference. Parser gains a minimal repeatable-flag seam: `parsedArgs.multi` collects every occurrence in order, `opts` keeps last-wins so single-value callers are untouched. Save's arg shape becomes positionals-then-flags (bootstrap's shape); a dash-leading second positional now dies on usage instead of being taken as a channel name. Suite green both packages; linux amd64+arm64 compile gate PASS (GOOS=windows fails on main pre-existing -- close.go/codexwrap.go syscall, fixed on the unmerged windows-port branch, untouched by this change); field-smoked through the built binary against a temp store (save/show/flagless-resave/refusal) and read-only against the real store (14 envelopes list, rn-foundry renders).

[2026-07-24 05:06:40 UTC] [Feat] `0d41228` feat(ledger): durable channel ledger + formation_run_id (bdx-mec.2) -- formations now self-index instead of needing cross-transcript reconstruction, which is how the api36 formation's alias-to-session map ended up existing nowhere. Append-only ledger at `CBUSDir()/.ledger`, outside every peer dir so PruneChannel can't reach it and dot-hidden so an older binary ignores it by construction. Seven closed event kinds wired to their store call sites: join, leave (self and reaper-emitted for crashed peers), rename, spawn, resume, restore, rebind; AppendLedger rejects unknown kinds and events missing channel/alias rather than dropping them; base fields serialize even when empty (known-absent, not missing); Emitter (self/reaper/forced) carries authorship separately from origin's validated fresh/fork/joined. Run identity is a per-peer claim file, not a channel record: a run exists only while a live peer claims it, which kills the stale-run-file class at the root; a historical (alias,sid) binding never proves current membership (session ids survive resume), the claim is the authority not evidence (a failed write is blank, never a fake nonempty id; a claimless peer's later events stay blank not sibling-inferred), and a split roster is unknowable across join/save/events (blank + warning, never first-wins). Mint lock is flock(2) on an fd after three rounds of hand-rolled file dances each leaked a new race. formation_run_id added to the snapshot at envelope + per-peer level, additive under the unchanged cbus-formation/v1 schema (old binaries carry it via Extra). Both reviewers signed off (design conformance + correctness), 6 review rounds, revert-audit on every test. Suite 7/7, -race clean, cross-compiled darwin/arm64 + linux amd64/arm64. NOT installed: running binary stays v0.8.1; additive + append-only means a v0.8.1 process never reads the ledger. Not pushed.

[2026-07-22 05:49:29 UTC] [Fix] `6c6b85a` fix(codex): claim listenership at join and print teardown cause last -- two field-driven fixes from cbus-6ij.5's parallel-review experiment (the codex peer `harness/sol` reviewed alongside the authoritative reviewer and found a real bug the reviewer's own mutation coverage had missed). Arming witness: `cbus codex` now claims listenership at join, before the bridge finishes arming, so `cbus list` shows the peer as live at once instead of "off pid=?" for the tens of seconds arming takes -- a live TUI was actually killed by a sweep that misread that window as dead. The claim seeds the replay cursor at offset 0 first and verifies the round-trip through `readCursor` before recording the pid+start-time witness; committing the witness before a seed that independently failed would have left listenerPid-set with the cursor absent, which `resolveResume` reads as an ever-armed migration and seeks to EOF, silently dropping every queued message -- seeding first makes that loss state unreachable by construction (closes sol-HIGH). The displacement gate now exempts the exact same process (pid and start both self) so the wrapper's own same-process arm isn't refused as a second listener; one-arm-per-process becomes a caller-owned invariant with no runtime enforcement, and the contract comment says so explicitly (record-only, sol-MED-2). Teardown order: `killServer` reaps the app-server first, bounded with SIGKILL escalation, so its dying WebSocket-reset stderr flushes before the bridge's own cause line, which now prints last -- both reap waits are bounded so a wedged app-server can't suppress the cause forever (closes sol-MED-1). Full suite + -race PASS, linux amd64/arm64 compiled in the golang:1.26-bookworm container, all pins mutation-verified, reviewer spot-checked the landed contract-comment wording against the orchestrator's exact phrasing. No session trailer. Not pushed.

[2026-07-22 00:16:55 UTC] [Client/Multi-harness] cbus-6ij.4 tranche B (multi-harness epic, increment 4, final tranche) -- `27bb2d9` feat(codex): codex-stop-hook fallback delivery for exec workers. This closes out cbus-6ij.4's build: the exec-worker delivery path where the app-server bridge isn't in play but Codex Stop hooks do fire. `cbus codex-stop-hook [--wait D]` long-polls the session's inbox(es) within the codex hook timeout and, on new chat traffic, emits `{"decision":"block","reason":<framed>}`, which codex injects as a continuation turn; no traffic before D (default 550s, inside codex's 600s timeout) or a `stop_hook_active` re-entry with nothing new allows the stop -- the timeout is a failure path, never used as a signal. Polls every registration (a worker hears each channel it joined), orders across inboxes by timestamp, batches new frames into one reason per fire with each frame intact for provenance, and skips presence/status like the bridge already does. A dedicated dot-prefixed `.stop-cursor` sidecar tracks delivered position, never shared with the follower's `.cursor`. Best-effort, exit 0 always. Docs: a Codex integration entry in command-reference.md with the `~/.codex/hooks.json` snippet (SessionStart -> hook-join, Stop -> codex-stop-hook, SessionEnd -> hook-exit) plus a pointer from multi-harness-exploration.md. Reviewer PASS, field loop independently reproduced (STOPHOOKOK verbatim, bounded exit). Full suite + -race green, linux amd64/arm64 built, no session trailer. Not pushed. **cbus-6ij.4 build is now COMPLETE**: both delivery paths shipped -- the app-server bridge for `--remote` peers (`8da523c`/`9ae66d6`/`d8ef18a`), the identity-scrub fix arc (`5ad7ff2`/`c0d5b70`), and this Stop-hook fallback for exec workers. Only cbus-6ij.5 (spawn/formation integration) remains in the multi-harness epic.

[2026-07-21 23:56:19 UTC] [Fix] `c0d5b70` fix(codex): scrub the app-server env too, the tool-shell execution locus -- closes the leak the 23:46:15 UTC amendment flagged. The prior scrub (`5ad7ff2`) covered only the `codex --remote` TUI process, but codex's tool shells (`cbus` among them) execute in the APP-SERVER process tree in the remote topology, and the wrapper still spawned the app-server with the launcher environment intact, so a model-run `cbus send` kept resolving the launcher's session id. `codexCommands` now builds both the app-server AND the TUI with the scrubbed `SessionID()` chain plus `CBUS_ALIAS`, so the actual execution locus falls to the alias path. Field re-gate PASS: reviewer independently reproduced `from=codexgate` under a poisoned launcher env (not just resolution-given-env, the actual live topology), per the doctrine set by the falsification. Single commit, no session trailer, full suite + -race green, linux amd64/arm64 built. Identity leak fully closed, field-proven. Not pushed.

[2026-07-21 23:46:15 UTC] [Amendment] The 2026-07-21 23:43:53 UTC entry below (`5ad7ff2`, codex identity-leak fix) is FIELD-FALSIFIED, not retracted-and-deleted but confirmed not to work: a post-land provenance round-trip against the relaunched live peer still replied `from=harness/orchestrator`. Root cause: in the `codex --remote` topology, tool shells (including `cbus`) execute in the APP-SERVER process tree, which the wrapper spawns with the launcher env intact -- `5ad7ff2` scrubbed only the TUI process env, the wrong locus. Neither the coder's CLI proof nor the reviewer's mini-gate caught it, because both tested resolution-given-env rather than the actual execution locus. Logged as the fourth exec-vs-remote topology divergence found today and a new gate doctrine: env-mediated properties in this topology need live-topology verification, the CLI door alone is insufficient. Re-fix in flight (scrub both processes, app-server primary); its changelog entry will follow separately once field-confirmed, per the append-only correction policy -- this entry corrects the record without editing the original.

[2026-07-21 23:43:53 UTC] [Fix] `5ad7ff2` fix(codex): scrub launcher session-ids from the codex TUI env -- found by the tranche-A live dogfood smoke (harness/codex joined the real "harness" formation channel), where codex's own `cbus send` inherited the wrapper's `CLAUDE_CODE_SESSION_ID`/`CBUS_SESSION_ID`/`GROK_SESSION_ID` and resolved the LAUNCHER's registration, spoofing provenance -- codex spoke on the bus as harness/orchestrator. `codexRemoteEnv` now scrubs the whole `SessionID()` chain from the TUI env and pins `CBUS_ALIAS`/`CBUS_CHANNEL`, so codex's cbus commands fall to the alias path and self-identify as the peer (the thread id isn't known yet at TUI spawn, discovery finishes later, so alias identity is the mechanism here, not a session-id env). Recorded v1 limitation: `--from` resolves to the bare alias, not a richer identity, via the pre-existing `CBUS_ALIAS` fallback. Standalone commit, full suite + -race green, linux amd64/arm64 built, no session trailer. Not pushed.

[2026-07-21 23:34:19 UTC] [Client/Multi-harness] cbus-6ij.4 tranche A (multi-harness epic, increment 4) lands Codex CLI as a first-class cbus peer, three commits: the ws-over-UDS bridge (802520e's --session-id sibling) speaks a stdlib WebSocket-over-UDS JSON-RPC client to a codex app-server, attaches via thread/resume, and delivers each framed bus message on a steer -> turn/start -> resume+retry ladder; a new frameSink seam tags frames by kind so the codex sink routes on kind instead of parsing rendered text, closing a spoofable --from presence-forgery defect found at pre-gate review (8da523c). A same-day fix rides out an async rollout-flush race on a zero-turn thread adopt that killed the bridge on every fresh-thread launch -- attach now waits bounded for the opener turn to idle before resuming, with a regression test reproducing the race (9ae66d6). cbus codex wraps both into a launch surface: stands up a per-peer app-server, launches a codex --remote TUI, learns the TUI's thread id from a passive server connection (SessionStart hooks proven NOT to fire in the --remote/app-server topology, so the TUI needs no hook config and no trust bypass), joins the bus as that thread, and bridges messages in; a dead bridge kills the TUI rather than leaving a deaf peer looking healthy. cbus hook-join ships alongside for the exec/stop-hook flows where hooks do fire (d8ef18a). Reviewer gate ran FINDINGS twice before PASS: first pass found the async-rollout race (fixed in 9ae66d6) plus a silent-bridge-death gap (closed by the fail-whole-unit design in codexwrap.go); final pass found one small test-coverage gap on the resume-error gate, closed with a single test. All three commits verified standalone-buildable, full suite + -race green, linux amd64/arm64 built, real-codex 0.145.0 field smokes (verbatim chat round-trip, presence-skip, TempDir-fallback socket path, group-kill teardown) all passed. Not pushed. Multi-harness core seams (cbus-6ij.1) + this tranche now both sit on main.

[2026-07-21 21:17:31 UTC] [Client/Multi-harness] cbus-6ij.1 (multi-harness epic, increment 1) lands harness-neutral identity + liveness seams in three commits: SessionID() becomes an ordered lookup (override > CBUS_SESSION_ID > CLAUDE_CODE_SESSION_ID > GROK_SESSION_ID) with a new OverrideSessionID(sid) restore-func, HookExit/HookCompact drop the os.Setenv round-trip in favor of the override (closes a stray-CBUS_SESSION_ID shadow trap), hook stdin decode accepts session_id or sessionId, snake wins when both present (802520e); --session-id flag lands on join/leave/rename/send via applySessionIDFlag, with warnIfSessionless relocated to after the override so the flag no longer triggers a false sessionless warning (21f1b37); isClaudeName becomes pure isHarnessComm(base) over {claude, grok, xai-grok-pager, opencode, codex} plus the claude- prefix, applied to both kernel comm and argv[0] in the ownerPid walk (cc115b9). Orchestrated as a dogfooded formation on the "harness" cbus channel (orchestrator/coder/reviewer/documenter). Reviewer verdict was FINDINGS, not a clean PASS: F1, the stray-env-vs-hook-stdin trap had no pinning test, proven by reverting HookExit to the setenv round-trip and watching the suite stay green -- fixed (TestHookExitStdinBeatsStrayCbusEnv + HookCompact mirror) and closed before merge. Full suite green, linux amd64/arm64 cross-compiled, CLI door matrix verified, six mutations killed on aimed assertions. Not pushed; gates the cbus-6ij.4 codex bridge next.

[2026-07-21 19:38:43 UTC] [Docs] design-space.md gains Sec 7 "The injection boundary generalized: foreign harnesses" -- reconciles the file-first doctrine with the Codex app-server push path proven by the cbus-6ij.4 round-2 probes (0.145.0: Stop-hook park chains indefinitely + timeout raisable past 600s; app-server unix socket is ws-over-UDS; external turn/start wakes an idle thread and renders in a live --remote TUI). Core argument: constraint 3 was always a measurement of the harness, not a design choice -- the bus stays files for ALL harnesses, only the last hop terminates in each harness's native injection surface (Monitor stdout / UDS push / plugin prompt); the per-peer app-server is codex's own machinery (per-session lifecycle, no bus state, one-peer blast radius), not the Sec 5.1 broker. Also: relay untouched (bridge = second consumer of the existing ws leg), the three reachability tiers (spawned / wrapper-launched+adoptable / plain = send-only, retrofit via codex resume relaunch), provider-seam extraction deferred until a third harness ships. multi-harness-exploration.md Sec 4 verdict superseded by a dated update block; its M6-stale liveness/argv note struck.

[2026-07-21 06:22:02 UTC] [Docs] New docs/architecture/design-space.md -- store/transport design-space rationale captured from a v0.7.0-era design review: the three pinning constraints (durable store-and-forward by construction, no daemon, Monitor boundary), the idle-poll cost ledger, the --steal takeover-cadence reasoning, the harness truncation funnel (1 MiB protocol cap vs ~3 KB display), and the rejected-alternatives analysis (local ws broker, p2p ws, RPC/IPC family, SQLite, Redis Streams). Cross-linked from overview.md's companion list and prior-art-and-cc-internals.md Sec 5; all code anchors symbol-verified.

[2026-07-20 03:51:31 UTC] [Client] v0.7.0 -- M6 deprecation-drop, closes cbus-8k9.4 (go-port epic): (1) M6.1 (`75e352d`) drops the deprecated `register` verb and `peers` alias -- both now fall through to the frozen `cbus: unknown command '<X>' (cbus --help)`, exit 1; zero programmatic callers meant no compat shim was needed. (2) M6.2 (`9a3a075`) deletes the `TRANSITION(P3T2)` argv-grep liveness shim whole (`liveness_transition.go`) -- `listenerIdentityHolds` has one branch left, the structural `(pid, starttime)` witness; an armed meta with no witness now reads dead outright. Field impact verified nil before the drop: every armed meta on the Mac carried a `listenerStart` (9 of 9), the NUC's store held no peer metas at all. FLEET COMPATIBILITY: a pre-P3 (pre-v0.4.0) binary arming against a shared `CBUS_DIR` after this upgrade writes metas the fleet reads as dead -- the argv read shim is gone and listener identity is structural-only; fleet binaries must be v0.4.0+. `TestClosePeerIgnoresAForeignListenerPid` removed as VACUOUS (cbus-p8g) -- it kept passing after the collapse while pinning nothing, since its stampless fixture read dead for an unrelated reason once the branch it exercised was gone; its contract is already carried by `TestClosePeerIgnoresARecycledListenerPidStructurally`, closed MOOT at merge (reviewer ran the pin directly in the linux container; inheritance chain recorded in `9a3a075`'s own commit message). (3) `cbus-fi3` (`1601f13`, test-only): the golden-row pid normalizer replaced only the digit run inside a fixed-width `pid=%-7s` field, so the goldens silently only held for 5-digit pids -- darwin usually obliges, a container hands out 2-4 digit pids and the M6.2 gate run failed there on all four pid-bearing subtests. Fixed by normalizing the whole padded column; the placeholder pads identically to a 5-digit pid so all four golden constants stayed byte-identical. Lesson: a normalizer owns the whole column, not the value inside it. Docs: command-reference.md sweeps every register/peers mention (dispatch table forward-pointers, the Reserved-names bullet, the retired `cbus register` subsection, `list`'s dropped `peers` alias heading, section 15's table + port note) and behavior-spec.md (the dispatch-table row, the quirk-registry item, a new TRANSITION(P3T2) closure note in the preamble); port-map.md's Phase 3 bullet and D1 ruling marked fully DONE; compat-deletion-plan.md gets Tranche 3 and is stamped CLOSED (all seven original items have a final disposition). cbus-8k9.4 epic closed with this release.

[2026-07-20 03:01:14 UTC] [Client/JSON] v0.6.1 -- cbus-vjo, field finding post-v0.6.0: `list --json`'s unfiltered path emitted a peerless directory in the store root (e.g. `$CBUS_DIR/roles`, written by `install-roles` beside the channels) as a phantom channel with an empty `peers` array. The zero-peers drop already existed but was gated on `--active`; text `list`, `channels`, and `channels --json` all already agreed on dropping it unconditionally, so `list --json`'s unfiltered path was the lone dissenter (`8ee5dbc`). Found by running the real binary against a production store rather than a fixture -- the regression fixture mints the peerless directory via `install-roles`, the actual door that creates it, since every earlier test channel came from `join`, which always writes a peer alongside the directory. Legacy v1 stays exempt from the drop (peerless by construction, predates the alias level, R18 wants it visible for the prune remedy). Docs: `behavior-spec.md` Sec 9.1 gets the parity note (`045a059`). Also lands one new standing doctrine in all four role files: bus message bodies are single-quoted in a shell `cbus send`, never double -- a double-quoted body command-substitutes backticks and expands `$vars`, live-observed twice this cycle (a documenter correction message ate its own text on an unescaped backtick, harmless; a reviewer gates message with backticks around an install-roles reference executed it against the real store, impact verified nil).

[2026-07-20 00:51:16 UTC] [Client/JSON] M5 (go-port epic, cbus-8k9.4, d4d34ac..5f746bb): `core.ValidStoreName` rejects leading-dot and leading-dash names where the client mints them (`Join`/`ReserveAlias`/`Rename`'s three store chokepoints, plus `branch`/`spawn`'s `--name` and channel pre-validators, now share one predicate instead of five ad-hoc `HasPrefix` checks) -- additive over `ValidName`, which stays the wire authority the relay gates `/send`/`/tail` on, so an older or third-party client's bad name still arrives and can still be addressed (`ParseLocal`/`ParseRemote`, `rename`'s channel selector, formation `Validate`, and the remote marker tree are untouched per R20 -- `cbus unregister <ch>/.foo` still works). `branchChannelFromGit` sanitizes instead of rejecting, so a repo at `~/.dotfiles` keeps deriving a channel. `list` and `channels` gain `--json` (`client.ScanStore` is the one traversal both the text renderer and the JSON encoder walk, so they can never disagree about who's listening -- deliberately not `ChannelRoster`, which drops a torn-meta peer instead of keeping it with blank fields); `schemaVersion` 1 envelope, object-at-every-level forward compat, `legacyV1` rendered explicit with an empty `peers` array, `listenerPid` omitted (never `0`) when never armed, `scope` pinned `"local"`. `--json` refuses a relay target loudly in both flag orders (`list @host --json` and `list --json @host` each reach a different silently-wrong answer unguarded) rather than answering the wrong question; `active <ch>@host --json` inherits the same refusal. `whoami --json` emits one document shape whichever state the session is in -- an unjoined session gets the same keys with empty `local`/`remote` arrays, never a different document, and the exit-1-when-empty probe semantics scripts already branch on are preserved; `local`/`remote` are separate keys since they're different identities (local has no host, a remote from-default marker always does). Absorbs cbus-oq9.4 (closed). Docs: command-reference.md's Name validity section documents the tightening inline (mirrors name.go's own two-of-four-admissions-tightened framing); new `--json` subsections under `list`/`channels`/`whoami`; behavior-spec.md gains a new Sec 9.1 JSON output contract section. Three new standing doctrines in roles/reviewer.md: an absence claim from a partial read is not evidence -- a range anchored at a symbol's declaration excludes the doc comment above it (M5.2b's n2 citation-missing finding, retracted); a mutation verdict is evidence only after the mutant is proven on disk, not just that a test ran green after (M5.2a: a heredoc replace silently no-op'd on tab-vs-space indentation, and an unchained shell command still ran against unmutated code); and exit-code assertions must run the binary bare or read PIPESTATUS, never `$?` after a pipe (M5.3: `... | jq -c .; echo rc=$?` measured jq's rc, not whoami's, and nearly filed a correct binary as broken). All three self-caught by the reviewer before shipping as wrong findings. Reviewer CONDITIONAL APPROVE on the docs commit, two binding fixes folded in: F1, behavior-spec.md mischaracterized `ChannelRoster` as the rename-path lookup (it's the formation-save roster reader, no rename call site); F2, the all-digit-names bullet conflated legality (still open) with jset's int-coercion (bash-era only -- the Go port's `renameMeta` always writes `alias` as a string, a documented C-delta). Class-C fold: the stale "extra args silently dropped" line in the `whoami` section (wrong since the P2.5 harness layer, `581b1c7`) struck through. Doctrine 14 (absence-from-partial-read) ruled universal and propagated to roles/coder.md and roles/documenter.md as their own role-specific addition; doctrines 15/16 marked reviewer-confirmed. Same false-claim family, ruled in before the reviewer's confirmation pass: Quirk index item 11 also claimed `channels`/`whoami` silently drop extra args -- stale since the same P2.5 harness layer (`581b1c7`), split from the still-accurate `hook-exit`/`hook-compact` half.

[2026-07-19 19:23:43 UTC] [Client/Replay] M4 (go-port epic, cbus-8k9.4, e0ce7de..5c1fadc): durable per-peer `.cursor` sidecar (dev+ino+offset, temp+rename, one writer) replaces the null-listenerPid tri-state for local replay -- every re-arm (plain, post-rename, post-steal, or after a --force queued mail into a dead gap) now resumes from the last delivered frame boundary instead of seeking the inbox end, closing cbus-8no. Contract: loss is silent and unrecoverable, duplication is visible and self-evident -- every ambiguous branch resolves toward duplicates. Followers now carry a (listenerPid, listenerStart) self-identity witness (N2, 33bd5e2) and re-check it before every inbox reopen, closing the cbus-0r8 foreign-reopen leak (behavior-spec.md's "zombie reattach") via R14's frozen NOT-MINE-on-doubt polarity; a local displacement gate + --steal (N3, 7ede690) refuses a second arm on an already-armed alias, relay-style. Dormancy markers now name the remedy that actually works per cause (04dfbc8) instead of a uniform "re-arm" that was wrong 3 of 4 times. Reviewer findings fixed same-session: F1 (5c1fadc) replaced an unreachable causeRenamed branch (the claimed cleared-witness-at-old-path mechanism was never true -- retracted) with real findRenamed sibling-scan detection; F2 (c4b8743) fixed the cursor persisting raw fd-consumed bytes instead of the last frame boundary, which could resume mid-frame and lose a message head, plus split cursor-read-error handling (ABSENT vs CORRUPT) so an unreadable cursor replays instead of skipping. D7 sessionless stderr warning ships with N3 (join/send/local-tail only), not at cutover. b1: identity check 17.7us, cursor write 114.5us, quiet follower writes nothing. Docs: behavior-spec.md Sec 8.6 rewritten as an 8-row resume table (rename/steal/dead-gap deliberately not special-cased), Sec 8.7 gets a Go-side closure note; port-map.md D4/D5/D7 marked DONE; command-reference.md and overview.md's stale cbus-8no/no-collision-gate/best-effort-force claims struck through with closure notes. Three new standing doctrines in roles/reviewer.md + roles/coder.md (red-test-must-fail-on-the-aimed-assertion, hand-built-fixture-must-have-a-real-writer, no-bare-pkill-on-live-infra) plus doctrine 2's prune case; ship in-repo now, installed fleets get them at the next selfupdate release.

[2026-07-19 05:48:18 UTC] [Client/Liveness] P3 compat tranche 2 (go-port epic, cbus-8k9.4): structural liveness replaces argv-grep for any peer this binary arms. New (pid, starttime) identity primitive (procStartTime, 3865d52; single-sourced token composers, 4337f79 B1) drives listenerIdentityHolds, exclusive-by-construction with a TRANSITION(P3T2) argv fallback that only answers for metas armed by a pre-P3 binary and is scoped to one release (removal rides the register/peers drop). Follower now runs fully in-process -- 4d53d4e M3 deletes the Decision 2 re-exec whole (TailArgv, ParseTailFollower, compatInboxPath, the hidden --inbox/--from flags) -- so COMPAT items 1-2 are both gone; InboxPath is plain filepath.Join, and the one surviving raw-spelling grep (metaInboxNeedle) is now the TRANSITION(P3T2) shim, not a COMPAT artifact. Rename now deliberately clears listenerStart (port-map D1) instead of going stale on the argv needle by accident. F3 (f853ff2) fixed a zombie-listener regression the structural rewrite introduced mid-tranche -- a dead-but-unreaped process still byte-matched its own (pid, starttime) token -- restoring bash's pinned zombie=dead behavior on both platforms. Also: cbus-w33 (510b595) bounds every runOsascriptOut caller (pane split + geometry, i.e. spawn/branch/formation pane targets) to a real deadline on linux, where a forked dash descendant could hang a close sweep for its full 30s budget; the window/tab fork path (a separate, still-unbounded runOsascript) is tracked open as cbus-cih. Full pidfd/kqueue liveness stays out of scope, filed as cbus-6lv. Linux runtime evidence is linux/arm64 via a colima container, not the NUC; amd64 rides the NUC deploy gate compile-verified only.

[2026-07-19 04:30:20 UTC] [Client/Compat] P3 compat tranche 1 (go-port epic, first .4 commit): unarmed-grace liveness is now lastActivity-only -- the D3 mtime fallback is deleted, and a readable meta with no parseable stamp counts as past grace (pre-port relics become prunable; broadcast skips them). Dropped the CBUS_PYTHON env line from --help (second ruled delta vs the bash heredoc). Retired the bash artifacts bin/cbus + bin/cc-branch.sh and the p26 bash-differential harnesses (scripts/p26_{sweep,rollback}.sh); bash rollback is now git-history recovery only. COMPAT items 1-2 (follower re-exec + raw inbox spelling) stay until structural liveness replaces argv-grep.

[2026-07-19 00:55:31 UTC] [Docs] Fold-in: spot-check caught a third stale "relay strips kind" instance at command-reference.md:397-398 (the first pass fixed the presence table + quirk 18 but missed this bullet) -- reworded consistently; repo+dev-docs swept afterward, no other live copies found.

[2026-07-19 00:52:59 UTC] [Docs] Docs-audit remediation (solo round, no reviewer): 27-item punch list from a 3-auditor sweep vs shipped v0.3.0, all verified true and fixed -- README/command-reference/protocol/overview/bus-formation.md in repo (pane target, `cbus close`, relay presence-crosses-relay correction), 2 changelog mechanical fixes (missing `## ` header, one inverted entry pair), dev-docs index.md/architecture.md/behavior-spec.md/port-map.md direct-edited (orphaned SHAs, 70%-per-peer correction, release labels, broken anchors).

[2026-07-18 21:26:45 UTC] [Fix] `procZombie` (used by `cbus close`'s wait-for-exit loop) was darwin-only and broke `GOOS=linux` at `make dist` (40eaec2) -- every prior round-2 gate had run on darwin, so the linux cross-build was the first thing to actually compile it. Implemented for linux via `/proc/<pid>/stat`'s state field (`Z` = zombie; unreadable = not-zombie).

[2026-07-18 21:09:13 UTC] [Docs] Amendment: two 2026-07-18 entries claimed formation apply always splits panes off the applier's own surface (19:26:00 UTC dev-trio tab->pane, and 18:38:09 UTC pane fourth-target). Since chain-split anchoring landed today (see below), that's only guaranteed for the FIRST split in a run -- apply now anchors each split at the largest-area pane among the applier and the panes created so far, so later peers often split off each other, not the applier. `branch`/`spawn` are unaffected: their anchor is still always the caller.

[2026-07-18 21:06:17 UTC] [Fix] `cbus close` false-succeeded on every live Go-era peer (4b42f91, 4a32ddd, 42426e2): ownerPid derivation matched kernel `comm`, but the bun-compiled CLI's accounting name is its version string, not `claude`, so `ownerPid` recorded null and close reported "already gone" without ever signaling the process. Now matches argv[0]'s basename (comm kept as fallback), with a listener-ancestry fallback gated on the listener's own inbox path appearing in its argv -- otherwise a recycled listener pid under a DIFFERENT session could donate that session to the SIGTERM. Also bounds the surface sweep to a 5s deadline so a wedged AppleScript costs a wait, not a stall.

[2026-07-18 20:23:43 UTC] [Docs] Multi-harness exploration — docs/architecture/multi-harness-exploration.md maps Codex CLI, Grok Build, and OpenCode as cbus peers: coupling audit, per-harness receive paths, comparison, ordered increments.

[2026-07-18 20:23:27 UTC] [Client/Forking] formation apply's pane splits now chain off the largest-area candidate instead of always the applier (f35a9e4, c3a8540): each split picks the biggest pane among the applier and this run's created panes, ties going to the newest teammate so the applier stays big -- a self-balancing grid instead of the applier getting shaved down by every subsequent split. Envelope peers gain a hand-maintained `split: auto|right|down` field (`formation save` never writes it); an explicit direction maps to tmux `-h`/`-v` and skips both the main-vertical reflow and the 70% first-split sizing for the WHOLE run, since a declared layout must survive its `auto` siblings.

[2026-07-18 20:17:15 UTC] [CLI] New `cbus close <ch>/<alias>... [--force]` (f35a9e4, a4086eb): SIGTERM the peer's owning process, wait up to 5s, then sweep its leftover terminal surface once the tty reads dead (tmux kill-pane / iTerm2 close by tty match). `--force` escalates to SIGKILL after the grace. Already-gone reports success, not an error; registrations are left untouched for the SessionEnd hook and prune to reap. Refuses self, remote targets (local-only -- a remote peer closes on its own host), and any pid that no longer looks like a claude session (recycled pid guard).

[2026-07-18 19:26:00 UTC] [Formations] dev-trio starter template targets flipped tab -> pane (all 4 peers): a fresh apply now builds the CC-teammate-style split grid in the applier's own window/tmux-window instead of four loose tabs. Tradeoff accepted (Carlos-ruled): pane hard-errors on hosts with neither $TMUX nor $ITERM_SESSION_ID (e.g. plain SSH), where the template previously degraded to tabs -- per-run escape is apply's per-peer target override in a copied envelope, or tmux on the remote end.

[2026-07-18 19:17:24 UTC] [Client/Forking] Pane/tab fork fixes (ff7ceef, folds into f246e3b): tmux `split-window`'s `-l 70%` sizing needs tmux >= 3.1 — a failed first split now retries as a plain (unsized) split before failing the fork, since the pane matters more than its width. tmux split failures now surface tmux's actual stderr text (`cmdStderr`) instead of a bare `exit status 1`. `osaForkITerm` now `os.Remove`s the self-deleting launcher tmpfile whenever DISPATCH (the osascript/pane/tab call) fails — only a failure during setup (write/`Close`/`chmod`, before dispatch runs) can still leak it. command-reference.md quirk 36 reworded to match — its launcher-leak claim went stale one commit after `72bd863` landed.

[2026-07-18 18:38:09 UTC] [Client/Forking] New `pane` fork target for `branch`/`spawn`/`formation apply` (f246e3b): splits the caller's own terminal surface instead of opening a new window — tmux-first when `$TMUX` is set (`tmux split-window` targeted at `$TMUX_PANE`, 70/30 width on the first split, `main-vertical` layout re-applied past two panes), else an iTerm2 osascript split of the caller's own session located by `$ITERM_SESSION_ID`, else a hard `cbus:` error (refusing beats silently splitting whatever's frontmost). Same commit fixes `tab`: it now targets the window OWNING the caller's session via that same UUID lookup instead of always the current/frontmost window (the tab-in-wrong-window bug); current-window survives only as the no-UUID fallback, and only there does tab still need an existing iTerm2 window. `window` unchanged. Docs updated: bus-spawn.md/bus-branch.md/CHEATSHEET.md target enums, command-reference.md §9 (mechanism line, both target headings, target-validation prose, tokenizer-quirk dispatch note) and quirk index item 36 rewritten for the corrected tab behavior; dev-docs deferred to a post-merge patch note (feature unmerged).

[2026-07-18 18:35:17 UTC] [Core] New `cbus hook-compact <pre|post>` verb (19dd20b, 4dd092d): PreCompact/PostCompact hooks broadcast a `compact-pre`/`compact-post` kind=presence event to every local channel a session is joined to, so peers learn it's about to lose (or just lost) in-context state — mirrors `hook-exit`'s SessionEnd wiring but leaves registration untouched. Local-only: the frozen POST /send contract carries no kind field, so a relayed notice would arrive as plain chat; wire change + relay redeploy deferred. Trigger text is allowlisted (manual|auto only, never passed through); PostCompact's compact_summary is never carried (unbounded conversation content). PostCompact chosen over the also-documented SessionStart(source=compact) alternative for wiring symmetry with PreCompact. Always exits 0 and drops trailing args past phase — not to dodge rc-2 blocking (a die here would be rc 1, which blocks nothing) but because a hook must never fail: either hook exiting nonzero surfaces a hook-error notice plus the first stderr line to the user, not PostCompact-only. Wiring stays manual and Carlos-gated (settings.json is shared across every CCS profile via ~/.ccs/shared) — no installer or cbus verb touches it. Tests: per-channel broadcast, both phases, trigger allowlist, env fallback, no-session no-op, bad phase, remote markers untouched, stderr-silence on both success paths, plus a users-door test driving the real binary with documented hook payloads. Docs: protocol.md (new presence events + local-only rationale), command-reference.md (new §7 subsection + quirk index), README/CHEATSHEET, dev-docs living pair (architecture.md + index.md).

[2026-07-18 04:12:19 UTC] [Docs] Docs-refresh effort complete (cbus-yle): dev-docs→repo promotion + F5 + ledger. Checked the prior that dev-docs architecture.md's 11 design-decision WHY rows were the strongest promotion candidates by grepping each against the refreshed repo tier individually rather than trusting the count: 7 were already duplicated by M1-M3, leaving 3 genuine gaps ported in `e2c7832` — the `dev-trio` starter's 4-peer count (name says three), formations' plan-before-launch rationale, and install.sh's deletion-as-policy rationale (delete, not deprecate, because a runnable bash-restore beside a self-updating client is a live footgun). F5: the plan-before-launch claim, as it traveled documenter-write -> orchestrator-paraphrase -> coder-transcription, drifted into "a refusal can't strand a half-launched fleet," implying a refusal halts the sequence — wrong; `formation_apply.go` shows a refused peer is skipped and launchable siblings still fork, only an all-refused plan aborts pre-launch (D13). Reviewer caught it against the real code path, fixed in `8779444`, and the coder adopted a new rule (verify a paraphrased claim against source before writing it) the reviewer confirmed matches its own rule 9. The documenter found the same wrong framing at its own origin — dev-docs architecture.md's design-decision row and index.md's formations paragraph both had it — and corrected both in this pass. Also applied the ruled dev-docs port-map.md §4.12 sync-back (tokenizer wording, 2 mentions) to match repo's D30 fix. Ledger: 14 repo commits (M1: 17a3ab1/f13ab49/2951c0a/8703a82/83cfe64; M2: f628283/de59683/ad45c78/33b4565/bdc2249; M3: be50b2f/e4a2849; promotion: e2c7832/8779444), 4 dev-docs files refreshed direct-edit, F1-F5 confirmed, n1-n7 on record. Both tiers now current against everything shipped 2026-07-14→07-18.

[2026-07-18 03:58:02 UTC] [Docs] Repo docs/architecture refresh, M2 (cbus-yle): protocol + compat-deletion-plan + cutover-package + port-map (f628283/de59683/ad45c78 APPROVED, 33b4565 CONDITIONAL + F3/F4 fold bdc2249): `f628283` documented protocol.md's Go-only meta fields (`origin`/`model` birth-record, `lastActivity`), all `omitempty` for bash byte-compat, plus the join stamp map (reservation-reclaim inherits, resume-rejoin preserves, takeover reads `joined`) and why `resume` never reserves (would clobber preserved origin). `de59683` and `ad45c78` closed out the retired-installer paper trail in compat-deletion-plan.md and cutover-decision-package.md — rollback is now a manual `bin/cbus` copy, not `./install.sh`. `33b4565` tightened port-map §4.12's tokenizer rationale (iTerm2's `command` param specifically) and reframed NUC propagation for the post-distribution world, but overreached: F3, its claim that the release flow meets all three installer-design goals including SessionEnd hook wiring, is false (`get.sh`/`selfupdate` touch no settings; wiring stays manual) — dropped from the "met" list in `bdc2249`. Same fold carried F4: overview.md's presence row still said remote presence doesn't work and the relay strips `kind`, stale since `cbus-ijx.5` shipped 07-14 (the relay renders `kind` and generates join/departed from the ws lifecycle now; only ijx.5 phase 2 client-originated leave/rename is open) — reworded. Dev-docs tier's own mirror of F4 (behavior-spec.md §5) got the matching `[SUPERSEDED 2026-07-16: ...]` annotation in the same pass; its port-map.md M10/Phase-2 hook-wiring rows were checked against F3 and ruled not a mirror (they truthfully describe the P2-era installer, a different claim). Docs-only, no code/test surface touched.

[2026-07-18 03:56:39 UTC] [Docs] Repo docs/architecture refresh, M3 (cbus-yle): README fixes + architecture-docs pointer (be50b2f APPROVED, e4a2849 APPROVED; reviewer reproduced R1/R3/R5 — skills exist on disk, all links resolve, meta.json anchor resolves, fences balance): `be50b2f` fixed the intro's `/branch-term`→`/bus-branch` mixup (the README's own example depends on the bus-aware skill, not the generic non-bus-joined fork), added `CBUS_REPO`/`CBUS_UPDATE_CHECK` to the CLI-reference env block, and flagged the `meta.json` field list as non-exhaustive (birth-record `origin`/`model` now exist). `e4a2849` added a distribution/self-update block to the CLI reference (`selfupdate`, `install-commands`, `install-roles`) and a new "Architecture & reference docs" section — the README's first-ever link into `docs/architecture/`. Docs-only.

[2026-07-18 03:50:50 UTC] [Docs] Repo docs/architecture refresh, M1 (cbus-yle): help text + overview + command-reference for spawn/formations/distribution (17a3ab1 APPROVED, f13ab49 APPROVED, 2951c0a CONDITIONAL + F1/F2 folds 8703a82/83cfe64): `17a3ab1` fixed `usage.go`'s formation-save help (origin/model DO come from the birth-record, contra the old claim that the store records nothing). `f13ab49` rewrote `overview.md`'s component map and added a new terminal-coupling section (`OSAForker` iTerm2 window/tab vs terminal-agnostic tmux). `2951c0a` rewrote `command-reference.md` with spawn/formations/distribution sections and renumbered the historical/quirks tail by two. F1: both docs falsely quoted "quoting cruft" as `port-map` §4.12's own words claiming it still mislabels the launcher shim — false on both counts (§4.12 was corrected at `dab6726` back on 07-13; the phrase never appeared there, it's `harness.go`'s paraphrase) and confirmed by three independent checks before the fold. F2: quirk 36's tmpfile-leak half is current Go behavior, not bash-era as its heading implied; split accordingly. Docs/help-text only, no behavior change; dev-docs tier (same effort) already matched these facts and needed no rework.

[2026-07-18 03:03:18 UTC] [Distribution] Legacy installers retired (de07cbe): `install.sh` (bash-client restore) and `install-cbus-go.sh` (transitional side-by-side installer) removed from the tree — releases + `cbus selfupdate` replaced both, and the first release had shipped and live-verified on both machines before the removal. The retired bash client stays at `bin/cbus` until P3; rollback to it is now a manual copy or git-history recovery. README, `get.sh`'s header note, and `docs/RELEASE-CHECKLIST.md` updated to match; the `docs/architecture/` references ride the already-flagged cbus-yle docs-refresh. Carlos-directed, post-release — the decision the distribution effort's D24 ruling deferred to exactly this moment.

[2026-07-18 01:40:33 UTC] [Distribution] M5 — bootstrap installer, release checklist, and install docs, the distribution effort's final milestone (1a93755, APPROVED + C7 + c8; C7 fold 28ffbff, c8 fold cd3d1a0): `get.sh` downloads the release binary via `gh` and installs the skill commands and role prompts it carries — the first-install path, distinct from the retired `install.sh` and the transitional `install-cbus-go.sh`, neither touched. It reads the repo slug from `$CBUS_REPO` rather than baking one into the committed script, and refuses clearly when it's unset. `docs/RELEASE-CHECKLIST.md` is the S10 deliverable: the ledger of every gh-facing path that can't run until a real release exists (the selfupdate round-trip on both a Mac rename-swap and the NUC's tmpfs cross-filesystem leg, the bootstrap, the update-check poll, `make release` itself), with the quiesce-gated sequence, so nothing gh-facing gets mistaken for tested. README and CHEATSHEET learn the release install path, selfupdate, the sha-guarded install verbs, and the update-check opt-in — docs shipped with the feature, not trailing it. Binding finding C7: `get.sh --clobber` downloaded straight onto the installed `cbus`, so a re-run that died mid-transfer or fetched a short asset destroyed the working binary. Fixed in 28ffbff: fetch to a sibling temp on the same filesystem, verify it runs, then atomically move it into place — the same never-damage-a-working-install order `selfupdate` already follows. Coder's own self-report, carried rather than smoothed over: this was its own M3 discipline (temp + verify + atomic move) not carried over to the sibling `get.sh` surface, found and fixed inside the same review cycle. Reviewer confirmed live: planted a working binary, ran a failing `get.sh` against it, and the original survived checksum-identical with its trap-cleaned temp gone. Class-C c8: the asset-name format lived in a third, unguarded place (`get.sh`'s own `BIN="cbus-${OS}-${ARCH}"`); closed in cd3d1a0 by extending the shared asset-name pin to grep `get.sh` too — reviewer's own mutation confirmed it now fails the build on a format change there. Reviewer pass highlights: the S10 checklist verified complete against its own pre-registration, including a formations-live-smoke-on-the-released-binary line; S8 came back clean across the entire commit range; both `gh`-error remedies (M3's download path, M3's c6 view-path fix) were live-probed again here; docs were checked to match behavior in both directions (doc claims code, code does what doc claims). EFFORT LEDGER, distribution (cbus-7sg) complete: M1 ddf7527, M2 03a5514, M3 10c6768 (+ c6 fold 08c577b), M4 1f813c5, M5 1a93755 (+ C7 fold 28ffbff, c8 fold cd3d1a0) — 13 local commits, ddf7527..cd3d1a0; nothing pushed, no remote exists yet; the full suite ran green under `-race` throughout; every local (non-gh-facing) path was exercised, and every gh-facing path is named, not faked, in `docs/RELEASE-CHECKLIST.md` for Carlos's post-quiesce run.

[2026-07-18 01:34:13 UTC] [Distribution] M4 — opt-in update-available hint (1f813c5, APPROVED clean): with `CBUS_UPDATE_CHECK=1`, `cbus` prints one stderr line when a newer stable release exists and, at most once a day, spawns a detached `gh` poll that outlives the command to refresh an on-disk cache; unset, it does nothing at all. Everything is best-effort and silent on failure — a version hint must never break or slow the command it precedes — except the hint line itself, which is exactly one line, never more. Prereleases stay invisible (the same clean-X.Y.Z tag classification the stable update path already uses, reused rather than reimplemented), dev builds are never nagged, and `--json`, `selfupdate` itself, and `hook-exit` are excluded so the hint can't interfere with scriptable or hook-driven paths; with no repo configured it's a no-op. Reviewer evidence: the exclusion list's completeness was argued structurally, not by enumeration — the hint writes stderr only, so every stdout consumer is safe by construction, and every merged-stream consumer was checked individually and excluded (`hook-exit`, `--json` since it's scanned before any terminator, `--version` as defense-in-depth even though it's not merged, and the hidden subcommand the poll itself dispatches to). No-recursion holds by construction (the detached poll's own dispatch can't re-trigger the hint) plus a live 0.09s probe confirming it in practice. A garbage on-disk cache fails silently rather than crashing the command. Prerelease-invisibility and dev-never-nagged were both live-proven, not just asserted. Reviewer's own evidence-hygiene note, carried for the record: its first probe miscounted `list`'s "no peers registered" baseline output as a hint line, and it caught this by checking content before concluding rather than trusting a line count — worth keeping visible as the standard this effort's verification held to. M5 (get.sh + checklist file + docs), the effort's final milestone, held for its verdict.

[2026-07-18 01:27:28 UTC] [Distribution] M3 — selfupdate from the latest GitHub release (10c6768, APPROVED + class-C; c6 fold 08c577b): `cbus selfupdate` downloads this platform's release asset via `gh`, verifies it, swaps the running binary in place, and refreshes the installed commands and roles; `--check` reports without applying. The download is version-gated *before* anything is swapped — the new binary must report the exact tag asked for, so a corrupt or wrong-platform asset can never replace a working install, and a pattern matching no asset is a loud error, never a quiet no-update. `versionMatchesTag` is a pure, mutation-checked compare; both a wrong-version fixture and an unrunnable one are refused. The swap is a same-filesystem rename, with an ETXTBSY-safe cross-filesystem fallback, and a failed swap leaves the running binary untouched either way. The asset name is pinned equal to the Makefile's dist names (an S5 test greps the Makefile directly, so asset-name drift fails a test in either direction — Makefile changes without the client, or vice versa); tag classification keeps a prerelease off the stable update path. Post-swap, the NEW binary's install verbs are exec'd to refresh commands/roles (D27's loud-never-silent held here too). M1's deferred empty-slug remedy was exercised live, not just unit-tested. `gh` is reached through a seam so the flow around it is tested without a live release. Reviewer evidence: all three of the coder's own mutations were re-run, plus the reviewer's own S6 anchor-drop mutation; the EXDEV fallback leg — which macOS cannot trigger naturally, so it was forced deliberately, since the NUC's tmpfs `/tmp` is where this path will actually fire in production — was driven through both a success and a failure-leaves-dst case; `gh` genuinely reached a live, authed, read-only view past the slug gate, not mocked. Honest limit for the eventual wrap: the full `gh` round-trip and `get.sh` are declared, not faked, checklist material for M5; `runSelfupdate`'s actual swap path is deliberately not unit-driven (it would replace the test binary itself), so its pieces are tested in isolation instead. c6, closed in 08c577b: the latest-tag lookup captured only `gh`'s stdout, so a failed lookup reported a bare "exit status 1" with no reason — the mechanism is the same class the download path had already gotten right (passing `ExitError.Stderr` through) but the view path hadn't. Now both surface it; live-verified against a genuinely nonexistent repo, returning "release not found" instead of a bare exit code — the private-repo user's most likely error was, until this fix, the one with no reason attached. M4 held for its own verdict.

[2026-07-18 01:20:50 UTC] [Distribution] M2 — embed and install the /bus-* skills and role prompts (03a5514, APPROVED clean): a root `claudebus` package embeds `commands/*.md` and `roles/*.md` (D23 — `go:embed` can't cross `..`, so `cmd/cbus` can't reach them directly, and the module root can); two new verbs write them out, `install-commands` to `~/.claude/commands` and `install-roles` to `$CBUS_DIR/roles`. Each is sha-guarded: an unchanged file is left alone, a locally-edited one is skipped unless `--force`, a fresh one is written, and every outcome is reported per file so a skip is never silent (D27) — one edited file doesn't abort the batch, but the exit is non-zero when anything was skipped. An embed-count guard fails the build if a command or role is added or removed without updating the expectation, and it compares served bytes against repo source so a stale embed can't pass. Reviewer evidence: the stale-embed canary was proven honestly — the reviewer's test binary was compiled *before* editing `roles/coder.md`, so the prebuilt snapshot caught the real staleness rather than a contrived one; the doctrine block came back byte-identical across all four embedded copies as served (2753 bytes each), extending the "duplicated 4x by ruling" invariant provably into the binary itself, not just the source tree; a dest-replaced-by-directory failure probe printed per-file while the batch continued past it (C4/D27 landed in code). M3 (selfupdate) held for its own verdict.

[2026-07-18 01:15:55 UTC] [Distribution] M1 — release Makefile and repo-slug plumbing (ddf7527, APPROVED clean): a cross-compile Makefile (darwin/linux x amd64/arm64, unix-only — D25) stamps version and repo slug via ldflags, builds a `dist/` matrix with exact `cbus-<os>-<arch>` asset names, and carries a tag-gated release target that publishes via `gh` — written, never run here; the remote and first release stay gated after the quiesce window. `repoSlug` is baked at release time and empty in source (D26), so no personal slug lands in a committed file and the repo can change visibility without another history scrub; `$CBUS_REPO` overrides it at runtime, and an empty slug prints the remedy rather than guessing. No behavior change to any existing verb; the legacy install scripts are untouched. Reviewer evidence: the bake was proven from the reviewer's own matrix build via strings-grep in both directions (the string is present when baked, absent when not); the `-X` ldflags target's symbol existence was checked, closing a silent-no-op class where a renamed variable would stop taking the flag with no error; legacy installers confirmed byte-identical; the release-target refusal (tag-and-slug-gated) was live-confirmed with `gh` never reached.

[2026-07-17 19:22:17 UTC] [Client/Formations] M8 — the starter template library, formations v1's final milestone (1ef6435, b816153, ee6fdcd, 5ffed2f, 8d38d6c, ace7dd0, APPROVED + c5; c5 fold 356cef5): ships `formations/dev-trio.json`, a committed four-role starter (orchestrator, coder, reviewer, documenter) — all `mode=template`, no session ids, no drift anchor, no models; models defer to each role file's `MODEL:` line at apply time (opus, fable, fable, sonnet — D21, resolved into both the launch and the reservation stamp, so a save after apply records the real model). rolefile refs are deliberately unpinned, since a pin on a committed file rots at the coming history scrub. `apply`/`show`/`bootstrap` resolve a formation by name runtime-first, then repo templates (D20) — the opposite precedence from role files, whose canonical home is the repo — and the resolved source is printed so a shadowed template is stated behavior, not a surprise; the name==filename identity rule now guards repo templates too, and a torn runtime file stops the resolve rather than silently falling through. `--channel` retargets a run without editing the file: set once in memory before anything reads the channel, so every downstream read (presence check, roster, kickoffs, reservations) follows it, structurally verified — nothing leaks onto the template's own channel and apply writes no envelope file. `save` may read a committed template as its refresh base (inheriting rolefile refs instead of blanking to TODO) but writes the runtime store only, never the repo file — checksummed byte-for-byte unchanged before/after (H1). `rm` stays runtime-only: a repo-only name gets a loud refusal pointing at git (D22), so it can never delete a version-controlled file. `--dry-run` needs no joined applier or existing channel — a new user's first command works against an empty store; a real apply still requires a joined applier, and save still refuses to snapshot a channel that doesn't exist (fragility rider on record: this path is unreachable from a real apply by construction, worth remembering if that construction ever changes). A canary test fails the build if any committed template contains doctrine-block text or a personal path — verified to actually bite. Record-only: n16, a `--channel`-instantiated effort can't inherit the template's rolefile refs via save, since repo-base seeding keys on a name+channel match — a `--based-on` flag could close this if it stings in practice; n17, `formation list` stays runtime-only, so a fresh user sees "no formations saved" while `apply dev-trio` already works — a discoverability seam, design-consistent, recorded rather than fixed. c5 (356cef5) teaches the skill `--channel` and the starter templates — all four items verified, c3's earlier skill entry left intact, the doc's style choice accepted as file-consistent. EFFORT LEDGER, formations v1 complete: M1 through M8 all reviewer-approved; 38 local commits, f4553ee..356cef5; nothing pushed; full suite green under `-race` throughout; ships on the next Carlos-gated install.

[2026-07-17 18:48:40 UTC] [Client/Formations] M7 — the meta.json birth-record (cd71d43, 33a639a, 9368480, APPROVED + C4; C4 fold 311f86f): closes honest-limit #2 from the M6 wrap block — origin and model are now recorded mechanically on launcher-born peers, not left to a human's memory. `ReserveAlias` stamps origin+model into the placeholder at reservation time, before the child boots: `spawn` stamps `fresh`, `branch` stamps `fork` (the one place a forked transcript is actually made), and the child's join carries them into the real-sid meta. D18, a three-way join resolution: a reservation placeholder's record is inherited; this session's own surviving meta is preserved (a resume-rejoin keeps its birth-record rather than flipping to `joined`); any other name joins with an unknown model — another session's record is never carried, and a blank is never inferred into a value, so a torn reservation stays blank. The birth-record is read before the channel prune, since a resumed peer's meta carries a dead listener that prune would otherwise reap an instant before the record could be captured, silently losing it on restore. `origin`/`model` are `omitempty`, so rewriting a bash-era or pre-record meta stays byte-identical (9 real metas pinned) — same discipline as `lastActivity`. Apply stamps what it actually launches: a templated peer reserves `fresh`, a forked peer reserves `fork` (both mint a new sid, so nothing is overwritten); recording a fork as fork-born is what makes a later restore refuse to fork it again rather than template it, the same rule `branch` already follows. Resume stays reservation-free by design — it reuses the existing sid, and a reservation placeholder would outrace that id on rejoin and blank out the preserved origin; pinned by a test that fails the build if resume ever reserves. Save fills origin/model onto a peer by the existing fill-once rule (blank envelope field, present meta value, never over a hand-set value) — a spawn-born peer now saves with `origin=fresh` and its model already filled, so apply can resume it without the hand-edit the smoke's step 8 needed. G6 hardening (311f86f, C4): a hand-corrupted birth-record's skip used to live only in the report struct, silent at the terminal — it now prints, naming the peer and the offending value; and save's guidance line, which still claimed the store records nothing beyond alias/sessionId/cwd/machine at the exact moment it started reading origin/model too, now tells the truth about what's captured versus hand-maintained. Reviewer re-proved C4 with the exact command that found it; the old wording is now mutation-pinned absent. Net effect: origin/model are mechanical on launcher-born peers, resume needs no hand-edit, and R1's teeth (never fork across roles) are restored on tool-saved formations rather than resting on a human's memory. M8 (starter templates) is the effort's final milestone, released in parallel.

[2026-07-17 18:11:26 UTC] [Client/Formations] v1 live smoke — identity verification PASSED: the frozen 10-step lifecycle ran live (not dry-run) across 3 windows with a sonnet child, formation `fmsmoke`; coder reported PASS with teardown proofs matched, reviewer confirmed independently. Identity proof, the load-bearing claim: the resumed session quoted its own first-turn nonce back unprompted — only possible with real process continuity, not a fresh actor told the nonce. Structural proof: the reviewer verified FIRST and directly against the resumed peer's own transcript (sid-B), not the coder's summary of it — both the original and resume kickoffs land in one session file, which a fork could not produce (a fork writes a different sid's file); zero `--fork-session` strings anywhere in the transcript; the reviewer's independent nonce count matched the coder's. Scope stated precisely: exit codes and teardown cleanliness rest on the coder's summary alone, nothing load-bearing depends on them. Evidence epistemics for the record: transcript is primary evidence, a reconstructed kickoff is corroboration only. The smoke's real yield was D17 (apply's `--brief` flag never wired — plumbing existed, CLI rejected it, kickoff briefs went silently empty), a design-5.3 fidelity gap inside the already-closed M5, caught only by a live run after the review cycle had passed it. D17's fix rides its own commit and folds into M5's record once reviewer-confirmed.

[2026-07-16 19:24:22 UTC] [Client/Formations] M6 — bootstrap verb + `/bus-formation` skill (8317624, APPROVED + class-C): v1's verb set is now complete (save|apply|bootstrap|list|show|rm), nothing beyond design section 9. `bootstrap` prints one peer's first turn for a human to paste — the path for a peer apply won't launch (recorded on another machine), or for reading what a peer would be told before opening a fleet. It composes through the same prompt builder apply uses (parity by construction: `BootstrapPeer` returns `KickoffPrompt` directly, plus a content test), because two renderers drifting into briefing a peer differently depending on who started it is the kind of divergence nobody notices until it matters. It reads no live state, so unlike apply it does not skip a peer on another machine — that peer is exactly who it's for — but it still refuses exactly what the file alone proves wrong (R1/R2/D12, mode-gated): no world is needed to know a fork-born peer must not be resumed, and handing someone that prompt anyway reproduces the ghost-orchestrator failure with extra steps. A peer with no recorded role is asked to describe itself in its first reply, since the store can't capture a role and save won't invent one. The skill surfaces exactly the verbs that exist and steers to `--dry-run` before a launch. Live-verified: the rendered prompt carries the D15 advisory pin line and the no-escalation line exactly once. Layers compose, record-worthy: the reviewer's malformed fixture was refused by M1's validation before bootstrap's own output logic ever ran. The shared self-describe line also closed a role-less-peer gap the reviewer had noticed independently, one milestone early. Class-C c3 (the skill's frontmatter hint omits bootstrap, a one-line fix) folds in, closed in 52d7dc9: bootstrap added to the argument hint, parity verified across hint/body/usage for all six verbs. Record-only: n13, a 3-line mode-mapping block is duplicated between apply and bootstrap — extract on next touch, not now.

Honest limits stated at wrap, v1-wide, in the coder's own words: fork mode is permanently untested live, by choice — proving it live would require manufacturing the exact ghost-orchestrator scenario the design forbids, so it's exercised only via a fake forker in tests. Origin is unknowable at save time; R1's refusal depends on a human having recorded it, until a meta.json birth-record ships (Carlos-gated, not built). The alive-check proves "not alive on the bus", never "not alive" in any absolute sense. Rolefile pins stay advisory because the pending history scrub will invalidate every recorded SHA (D15). Limits named at wrap are worth more than limits discovered later.

[2026-07-16 19:21:15 UTC] [Client/Formations] M5 — apply: launch the missing peers and prove they answered (b57511d, 1cbdec3, APPROVED + class-C): sequential and anchor-first, so a formation whose members expect an orchestrator to be listening gets one; the whole plan is decided before anything launches, so a refusal can't arrive halfway through a fleet. Convergence is a round-trip and nothing else — each kickoff carries a nonce and asks for it back, and apply reads its own inbox for the answers with a bounded, deadlined file read (never an exec of the tail follower, which would block forever); a peer that never answers is reported failed. The three modes launch three different ways: resume continues a session as itself (no `--fork-session`); kickoffs tell a peer what it is — a fork is warned it isn't the original and must not act on its parent's unfinished work, and a template-degraded peer is told it lost the history it was meant to have. D11 self-find (b57511d): the applier is never itself a peer to launch — an orchestrator applying its own formation had its own alias planned for relaunch and then failed on its own reservation, so the common case (applying your own formation) couldn't work; fixed in the plan itself so `--dry-run` and apply agree. Second D11: a duplicate no-escalation line in template kickoffs, found by reading a rendered 7.6KB kickoff rather than by a unit test, now exactly-once per mode. Zero-launchable is a failure, not a quiet success, and fails with every peer's individual reason; `--dry-run` is exempt since its report IS the answer. Reviewer live-dry-ran against the real formations channel; honest mechanism note: a whole-store diff is racy on a live bus (its first residue check flagged normal inbox traffic as residue; a scoped check replaced it). Trap for the record: role fixtures were shadowed by this repo's real `roles/` files, since `LoadRole` checks the git toplevel first — a test passed for the wrong reason; fixtures renamed to non-shipping names. No live smoke yet — stays orchestrator-gated. Also lands a5b8b727: extracted the shared child-launch prefix (branch/spawn/apply had rebuilt CCS-instance detection three times; behavior unchanged, profile `""` still resolves to the caller's own). Class-C c2 (payload refs must render in envelope order, not sorted — reading order is the point of M1's preservation) folds in, closed in 8806efc: `payloadRefs` now walks raw JSON in envelope order; root cause was `map[string]RawMessage` making order unrepresentable, with a sort added for determinism the author's order already provided. `show` already preserved order via `json.Indent`; `drift_anchors` stays sorted deliberately (unordered facts, not authored prose); torn/non-object payloads pass through as written, now pinned. Mutation-verified via `TestPayloadRefsKeepsAuthoredOrder`. Record-only: n12, one poll tick costs O(inbox), bounded — revisit only at very large inboxes. D17 folded in (found by the v1 live smoke, after this milestone's review cycle had already passed): `ApplyOptions.Brief` flowed into the kickoff, but no flag ever set it, so apply always sent an empty effort brief while bootstrap carried one. Fixed in 6c3ea2f (`--brief` flag, `opts.Brief` wiring, usage, a client-level render test) + 2c5d2ea (a CLI-path test driving the flag through `runFormationApply` to a rendered kickoff — verified to bite: unwiring `opts.Brief` fails it while every in-process test stays green). Reviewer's self-report on why the review missed it: it read both halves — the `Brief` plumbing and the flag-parsing allowlist — but never joined them, because the earlier kickoff-content test injected the brief in-process rather than through the CLI; 6c3ea2f's own original coverage was parse-without-render plus render-without-CLI, the exact in-process blindness this names. Distilled doctrine, reviewer's words in substance: a content gate for a user-reachable feature must enter through the user's door at least once. The new `applyForker` package var was evaluated as the right trade (the `http.DefaultClient` idiom, no parallel hazard since cmd tests use `t.Setenv`). The arc worth keeping: the smoke found the gap, the self-report named the mechanism, the closing commit embodies the doctrine in a test.

[2026-07-16 19:05:44 UTC] [Client/Formations] M4 — the apply plan: every decision made before anything launches (a03128a, APPROVED + class-C): `BuildPlan` is pure — same formation plus same world always yields the same plan and launches nothing — so every prohibition is a table test rather than a live rehearsal, backed by a read-only world gatherer. Refusals each carry their own message: a fork-born peer resumed or forked would re-run its parent's intent under another name (D12 — an unrecorded origin can't prove it isn't that case, refuse over warn per the B31 F1 lesson); one transcript claimed by two aliases means one of them is wrong; resume on a session live-armed elsewhere is refused rather than silently degraded to fork (D14 — names the live location and both remedies, kill/re-point or a deliberate mode=fork copy). Machine matching is strict equality (D13); a skip names both values, and zero launchable peers is a failure rather than a quiet no-op success; drift is reported, never blocking (the snapshot is a cache, the ground is live). Live-driven read-only against the real bus: byte-identical CBUS_DIR shasums before/after, every refusal class fired on real data — including D14 firing on the coder's own live session, launchable=1 / refusals=3, zero writes. The D14 remedy line sent the coder back to re-read the design and fix a pre-commit bug of its own: the build had gated fork on liveness too, which would have refused the mode the design prescribes for a live original — the shipped guard is resume-only. Harness finding for future re-verifiers: copying live registrations into a scratch CBUS_DIR silently breaks liveness detection (the argv needle rebuilds from the scratch path), producing false negatives. Ships no CLI by design (D9) — `--dry-run`/`--only` reach users at M5. Class-C c1 (a precedence test-pin asserted on both claimants, including an origin=fork one) folds in without re-review, closed and reorder-verified in 68f81ed. Record-only: anchor-first launch ordering gates at M5 (n10); `liveSids`'s limit is honestly scoped in the code as-is (n11).

[2026-07-16 19:00:29 UTC] [Client/Formations] M2 — list/show/rm for saved formations (31f9501): show flags the two states that make a formation unusable as written — a recorded sid whose transcript is gone (STALE) or a peer with no brief to send (TODO); a peer tagged with another machine reads `unchecked` rather than `STALE` since this host can't see that host's transcripts and calling it stale would dress a guess as a finding. Transcripts are located by globbing the sid rather than rebuilding a project dir from cwd (avoids duplicating the munging rule; a moved cwd would otherwise read stale while `--resume` still worked). `rm` refuses path traversal; an unreadable envelope lists with its error rather than being skipped. D7: `internal/client/transcript.go` accepted as a 7th file — its live/stale/unchecked predicate is shared with M4's resume gating. The `mbp` vs `carlos-mbp` machine-value convention is deferred to an M4 ruling. Reviewer's CONDITIONAL APPROVE raised binding finding C2 — test-portability only, not product code: fixtures hard-coded one developer's hostname and a literal `"nuc"` as the foreign machine, so the suite failed on any other host (and inverted on a host actually named nuc). Fixed in b24cd70: fixture hostnames now derive from `ShortHostname()`, with the foreign value built by extending it so it cannot collide. Reviewer's own C2 citation missed a twin instance of the same bug in `internal/client/formation_read_test.go`; the fix's inclusion of that file was accepted as valid scope, not scope creep. Record-only: RoleTODO substring over-match is warning-only; an invalid profile skips the sibling root and reads STALE, consistent with the predicate's framing.

[2026-07-16 19:00:29 UTC] [Client/Formations] M3 — save: capture a channel roster into an envelope (61c25f9, APPROVED): the store records exactly four substrate facts per peer — alias, sessionId, cwd, host — and save owns only those; mode/onStale/target/addresses get declared defaults (template/template/tab/[]), and model/rolefile/role/origin/profile are left for a human to fill in. A refresh updates the four captured facts and never overwrites anything else, which is what makes re-saving at a milestone boundary safe; a peer in the file but no longer on the channel is kept, not dropped, since a paused effort is the main reason a formation exists. An unloadable envelope refuses rather than guessing at a rewrite. Folded in: 9b3d487 fixes a defect in M1's own emission code — `encoding/json`'s default HTML-escaping mangled `<`, `>`, `&` in hand-written text (a brief saying "if a < b" came back re-escaped), which breaks the tool's contract to preserve what a human wrote; fixed with `SetEscapeHTML(false)` carried through the custom marshalers. The coder self-found this in its own delivered code; the reviewer had explicitly considered HTML-escaping at M1's review and classed it harmless — that judgment is retracted in substance, since real hand-written text can and does contain those characters. Record-only: object keys still HTML-escape (a values-only fix, narrower edge case); `drift_anchors.git_head` anchors the saver's own repo and is advisory, apply reports drift loudly rather than trusting it; `SaveFormation`'s doc comment wording is ambiguous against the four refreshing facts (wording only, no behavior change).

[2026-07-16 18:51:03 UTC] [Client/Formations] M1 — formations envelope (f4553ee): `cbus-formation/v1` typed load/save at `$CBUS_DIR/.formations/<name>.json` with atomic temp+rename writes, validation, and an opaque by-reference payload the tool never interprets; hand-edited unknown keys are retained and re-emitted after the known fields (`fields()` single-sources emission order and the known-key set, with a reflection test that fails the build if a struct field is ever added without one), and a re-saved envelope reproduces itself byte for byte. D6: no converter for the old v3-draft snapshots — unsupported-schema refusal is deliberate. Reviewer's CONDITIONAL APPROVE raised binding finding C1: a formation's identity is stated twice (filename and `name` field), and `LoadFormation` didn't tie them, so a hand-copied file (e.g. `roles.json` copied to `backup.json`) silently clobbered the original on next save. Fixed in 85207af: refuse loudly naming both remedies rather than adopting the requested name at load (adopting would silently revert a hand-edited field); a rename is therefore two-half — set the field AND move the file, either half alone refused. Reviewer confirmed end-to-end through the CLI (roles.json checksum unchanged; the guard also caught the fix commit's own test fixture). An early framing floated during review — "a deliberate rename via the Name field still works" — was retracted before it reached the record: a field-only edit is observationally identical to the clobber precursor, so intent isn't recoverable from the file alone. Record-only: a WriteFile failure can leave a temp file behind (cosmetic); `drift_anchors` field order normalizes on first save; in the copy-then-mv-remedy case the user's own mv overwrites the original with the copy, which is correct (user-executed, tool touches nothing). `go test ./...` green repo-wide, 24 focused cases.

[2026-07-16 15:32:29 UTC] [Client/Spawn] New `spawn --role <r>` flag — reads the committed role prompt roles/<r>.md (spawn cwd's git toplevel first, then $CBUS_DIR/roles) and appends its body to the child's first turn after the join/arm instructions. Defaults the alias to the role name and the model to the file's MODEL: line; explicit --name/--model win; an unknown role fails before any alias is reserved, listing the paths it tried. `branch` refuses --role (a fork inherits its parent's intent; forking across roles is the documented anti-pattern). Live-verified: `spawn tab roletest --role documenter` launched with --model sonnet --name documenter and the doctrine block riding the prompt; child joined and armed as roletest/documenter. Also isolated TestSiteURL from fleet-configured shells (CBUS_SITE_NUC_URL). Commits 834b7ad, 9c3f4c8.

[2026-07-16 07:42:19 UTC] [Docs/Roles] New `roles/*.md` — four formation role prompts (orchestrator, coder, reviewer, documenter), each carrying a mission, a byte-identical shared standing-doctrines block (no cross-file references, so a prompt survives being pasted alone into a fresh window), role-specific process rules, a literal report/verdict template, and a MODEL: default line for `spawn`. Distilled from live go-port formation dispatches, where these rules previously existed only as ad hoc messages. Two follow-up commits closed independent review: scoped two doctrine claims (message replay on re-arm, stale-address sends) to the transport path they actually hold on instead of stating them as universal, added a dispatch-anatomy rule plus kickoff/review-request templates to orchestrator.md, a ruling-request form to coder.md, and corrected a "one milestone, one commit" absolute. A blind extraction cross-check between the two review passes found zero overlap in what each caught. Prompts only — no `spawn --role` plumbing yet. Commits 411c083, 4e0a497, b3a806e.

[2026-07-14 23:36:41 UTC] [Relay/Presence] Remote presence MVP (cbus-ijx.5) — join/departed now cross the relay, closing the local/relay asymmetry. Relay-only, no wire break, no mandatory client code change. Origination is connection-lifecycle: hub.attach emits `join`, a dropped tail emits `departed` after a grace window (new `presenceGrace`=90s, distinct from pongGrace; `-presence-grace` flag added to tune/test). Fan-out is spool-mediated (write an ordinary spool line with kind/event to each CONNECTED recipient + poke, same path as /send) so presence keeps FIFO order with messages and needs no per-tail plumbing; connected-only, offline roster stays `cbus list`. `core.Reframe` now renders ` kind=<k>` when a stored line carries it (kind-absent lines byte-identical; golden corpus has 0 kind lines) — converges Reframe with LocalEmit; updated the "kind dropped" divergence-matrix test to "kind rendered". Design scoped + filed to cbus-ijx.5 (unfrozen for this subset), reviewed with a Fable 5 session BEFORE and AFTER coding. Fable caught a real state-machine bug in the first cut (departed committed under lock but fanned out off-lock → a reconnect in that gap read present=false, decided a fresh join, and the two fan-outs reordered into a STUCK departed for a live peer). Fixed with decide-and-enqueue under mu + a single ordered drainer (decide-order == emit-order), plus a global-seq departure guard (departWait) that self-cleans the prior map leaks. Tests: 10 relay presence tests incl. BUG#1 ordering regression, concurrent-hub stress, e2e Reframe-over-spool — all green with -race (5x on timing tests). Validated live on a loopback relay (grace=2s): join on connect + departed after grace, both `kind=presence`, in order. NOT deployed to the NUC (relay restart would blip live android-sdk tails — needs a quiet window). Phase 2 (client-originated leave/rename, SessionEnd departed, durable offline catch-up, protocol version field) stays wire-touching and deferred. Follow-up: spawnPromptRemote could advertise the new pushed presence (client prompt string, needs a client rebuild).

[2026-07-14 06:50:11 UTC] [Relay/CLI] Added server-side relay prune — `cbus prune [<ch>]@<host>`. The relay spool is append-only (a peer's maildir is born on its first queued message and never GC'd), so off peers linger in `cbus list @host` forever and local `cbus prune` (which keys on pid liveness) can't reach them. New relay `POST /prune` endpoint (bearer-auth, optional `?channel=` scope) drops every spool peer with NO live tail AND NO queued mail — a peer with pending mail is always kept. Uses `hub.snapshot()` for liveness + new `spool.Store.Remove` (atomic same-parent rename to a `.prune.*` temp, recheck new/, restore if mail raced in — so a concurrent send is either fully spooled or surfaced to the sender, never silently dropped). `spool.Peers` now skips dot-prefixed dirs (matches local store; shields the temp). Client: new `core.PruneResponse`, `client.RemotePrune`, and `runPrune` remote branch (`runPruneRemote`) — channel-scoped like local prune; a trailing `/alias` is REJECTED (footgun guard on a destructive op, unlike list which ignores it). Tests: spool Remove/missing/dot-skip + relay handlePrune (channel scope, connected-keep, mail-keep, auth 401, method 405, bad-channel 400). Verified live over HTTP against a loopback relay: channel + host sweeps work, queued mail survives every pass, emptied channel dirs rmdir'd, alias-guard fires pre-network. Help/README/CHEATSHEET updated. Relay NOT redeployed to the NUC — ship in the quiesce window with the pinned binaries.

[2026-07-14 06:22:09 UTC] [Docs] Redacted docs/prior-art-and-cc-internals.md into a "declassified document" (aesthetic only, █ U+2588 full-block bars — works on GitHub; CSS black-bars don't and literal <redacted> would be stripped): §1 sibling-project names/handles/stars/versions (CCS KEPT — it's claudebus's own runtime), §2 body fully blacked (title kept, SendMessage gone), §3/§4 scattered redactions + §4 decision-log commit-hash refs, §5 the internal bd-dashboard-bug bullet + both scratchpad/ paths (cbus-foc/oq9 epic IDs left). Style not secrecy — the same CC-internals findings stay openly in README + overview.md; "see prior-art" pointers left as-is (a full docs reconciliation pass is planned in a separate session before the public push). This file's HISTORY gets the same redaction in the pre-push quiesce window — op #4 of the 4-op filter-repo scrub (email + hostname + Claude-Session-trailer-strip + prior-art-history), via a --blob-callback keyed on the doc's enumerated blob-ID set (git log --follow, not a title signature).

[2026-07-14 05:07:23 UTC] [CLI] branch/spawn children are now titled by their bus ALIAS (supersedes the channel-default half of e353af2, per Carlos): the parent pre-claims the child's alias at fork time — new `ReserveAlias`/`Unreserve` in store.go (placeholder meta, sessionId "reserved", null pids; child's explicit `cbus join ch alias` reclaims it, abandoned reservations die via the 10-min unarmed grace) — and passes the same value to the CC CLI --name. `--name <n>` now fixes alias AND title (ValidName charset — free-text titles gone); omitted = auto-pick (main/fork-N). Remote spawn: --name pre-assigns the relay alias in the prompt; omitted keeps child-picks + address-as-title. New aliased prompt variants (bootstrap/spawn local/spawn remote); `cbus bootstrap` gains optional [child-alias]; branch/spawn print the child address up front. Spawn's "mutates nothing" contract narrowed to "reserves only". Binary NOT rebuilt/deployed — pinned 1a5821d until the quiesce window.

[2026-07-14 04:50:45 UTC] [Port/Go] Generalized relay-host resolution — removed the built-in `nuc` host from BOTH clients (`SiteURL` in internal/client/endpoint.go and `site_public_url` in bin/cbus). Every relay host now resolves solely via its `CBUS_SITE_<HOST>_URL` env var; an unset override is a hard error (no built-ins). FIRST deliberate change to the retired bin/cbus rollback artifact — it now needs the env var too. Reworked endpoint_test.go's builtin assertions to env-override + UnknownHostError; annotated the built-in-table claims in command-reference/overview/protocol.md as retired; README gains a "cross-machine requires a relay / adding a machine" note + env-based endpoint wording. The `no built-in hosts` help line already landed in e353af2 (shared usage.go with --name); this commit makes it true. Prep for a public repo (keeps a personal relay hostname out of shipped code); MBP env prep (CBUS_SITE_NUC_URL in settings.json + zshrc) done so nothing hard-errors after a rebuild. Binaries NOT rebuilt — current builtin binaries stay live until the pre-push quiesce window. Paired with LICENSE add (6bd7038). Git-history hostname scrub + author-email rewrite deferred to that quiesce-window filter-repo pass.

[2026-07-14 04:42:50 UTC] [CLI] `--name <n>` flag on `cbus branch` and `cbus spawn`: title the child session via the CC CLI's `--name` (shows in the session picker, prompt bar, and terminal title — programmatic /rename equivalent, verified against CC docs/help). Defaults ON: branch children title as the channel, spawn children as the channel or channel@host; explicit `--name` overrides. Free-text names allowed (shQuote-safe); flag-shaped (leading `-`) rejected pre-fork like --model. extractModel generalized to extractFlag/extractForkFlags. /bus-branch + /bus-spawn updated. NOT yet pushed/propagated — holding for push-review.

[2026-07-14 04:05:15 UTC] [CLI] `--model <m>` flag on `cbus branch` and `cbus spawn` (b80d4a3 + skills 1a5821d): launch the child session on a specific model (passed through to the CLI's --model — sonnet/opus/fable today; token shape-validated, flag-shaped values rejected pre-fork to avoid the instant-close-window trap; accepted anywhere in argv). /bus-branch and /bus-spawn updated to pass it when the user mentions a model. Deployed MBP+NUC; per-role model defaults deferred to cbus-vj9 (role prompts).

[2026-07-14 03:20:41 UTC] [Port/Go] New `cbus spawn` + `/bus-spawn` — fresh session joined to a channel (post-cutover, implements frozen cbus-ijx.2). Like `branch` but launches a BLANK session (no --resume/--fork-session) whose opening prompt instructs it to join and arm the given channel itself, rather than resuming this session's transcript — local (join + Monitor tail) or remote (ws arm spec + the 1006 re-arm doctrine baked into the remote prompt). The spawning side mutates nothing — no join, no arm, no bootstrap print; it only launches the terminal and reports. Reuses ForkSpec/TerminalForker/the launcher shim wholesale (same iTerm2-tokenizer-safe mechanism P2.5 fixed). Local channel derives own-reg -> git repo name -> global; remote `channel@host` is explicit with no alias (the child picks its own, same as bash's remote-alias convention). Validation happens pre-fork (bad target/channel fails before any terminal opens). First deliberate help-text addition beyond bash parity (`--help` now documents `spawn`, which bash never had). 6 new tests. Deployed to both MBP + NUC binaries at this commit, `commands/bus-spawn.md` scp'd to `~/.claude/commands/` on both. Live-validated: a spawned session joined and armed `claudebus/main` on the first try. Commit c341c15.

[2026-07-13 22:02:54 UTC] [Docs] Post-cutover documentation pass (17 files, Fable-5-drafted map applied). Repo, 4 commits: (1) e9c6a84 README.md + CHEATSHEET.md — describe the Go client (re-exec follower not python, `go build` install with no python3/tail-F dependency, native branch/TerminalForker, --version verb, hook-exit + presence documented, 1 MiB cap) and fix 14 pre-existing stale claims catalogued in behavior-spec.md §12 (tail -n +1 -F, "nothing is lost" overstatements, relay "epic in progress", displacement duplicate-delivery claim, auto-resolved-from wording, and more) rather than inheriting them. (2) d5d7c61 architecture set — overview.md gets a banner + surgical component-table/mermaid/environment-assumptions edits describing the Go client while keeping the audit-era body as the verified historical record; command-reference.md and protocol.md get status banners pointing at the port's 10-item intended-deltas list, bodies left untouched as the frozen behavioral contract the port was verified against; port-map.md gets a status banner + DONE stamps on Phase 0/1/2. (3) 1ee6f46 commands/bus-join.md (softens the "nothing is lost" overstatement, re-copied to ~/.claude/commands/ on the MBP and propagated to the NUC by hand — NOT via install.sh, which would roll back the cutover) + install.sh (now documented as the LEGACY/rollback installer, header + closing message rewritten). (4) 485fe80 cutover-decision-package.md (tense-fixed to read as the executed decision record) + compat-deletion-plan.md (status stamp: item 5 done, items 1-4/6/7 pending P3). dev-docs (direct edits, no git): index.md, architecture.md, behavior-spec.md, port-map.md all stamped with the 2026-07-13 cutover status + the same 10-item intended-deltas list where applicable; behavior-spec.md §12's drift register marked resolved (only after the repo fixes landed). NO-CHANGE files per the map: docs/prior-art-and-cc-internals.md, commands/bus-branch.md, commands/bus-rename.md (still literally true of the Go client).

[2026-07-13 21:26:48 UTC] [Port/Go] Phase 2 complete: cbus-go local transport + follower + harness layer, full per-machine parity with bash, 6 milestones (P2.1-P2.6), all reviewer-approved, cutover-decision = UNCONDITIONAL GO (cutover itself still user-gated), HEAD 2aadf1a. Phase 2 (6/N) — cutover-readiness: installer, version stamp, help parity, decision docs (P2.6), CLOSED. `install-cbus-go.sh`: builds with a `-ldflags` version stamp (git describe/hash), places mode-agnostically (`rm -f` + `cp` — fixes M12: a `cp` through an existing symlink would clobber the repo source), reports SessionEnd hook wiring read-only; touches ONLY cbus-go — no bash-cbus path, no install.sh retarget, no settings.json writes. New `cbus-go version`/`--version` (a readiness delta bash never had). Help/error parity (ruling Option X): `usage.go` ports bash's `--help` byte-for-byte except the obsolete CC_BRANCH env line (branch is native since P2.5 — a ruled delta); unknown-command matches bash's single-quote + `cbus --help` form; cbus-go now self-IDs as `cbus` so cutover is a pure binary swap (the `cbus-go --help` coexistence oddity is documented in the decision package). New `docs/architecture/cutover-decision-package.md` (GO recommendation, per-machine swap steps, rollback procedure, what-stays-bash=nothing, evidence bundle) and `compat-deletion-plan.md` (the coexistence shims that delete at P3 homogenization). Evidence: Class A/B differential sweep 27/27 on BOTH MBP (darwin/arm64) and NUC (linux/amd64, temp-binary — nothing installed); rollback-safety all pass (bash reads+operates on cbus-go-written state incl. D3 lastActivity); installer validated incl. the M12 over-symlink case; `go test -race -count=1 ./...` green; ZERO cutover executed. RIDERS (2826de5): R1 — greppable `// COMPAT(P3 #N)` markers on every coexistence shim the deletion plan names (#1 re-exec in follow.go, #2 raw inbox spelling in follow.go/liveness.go, #3 mtime grace fallback in liveness.go, #4 the CBUS_PYTHON help line in usage.go), so `grep -rn 'COMPAT(P3' internal/ cmd/` enumerates them — comment-only. R2 — committed the P2.6 evidence under `scripts/` (was session-ephemeral scratchpad): `p26_sweep.sh` (Class A/B, every verb, 27/27) and `p26_rollback.sh` (bash reads+operates on cbus-go-written state); decision-package evidence bundle repointed to `scripts/`; deletion plan documents the grep convention. Both scripts re-run green from the committed location. SMOKE SAGA (638c20b + 2aadf1a): the earlier "session ended instantly" diagnosis was a fast-exit PROBE artifact, NOT a branch fault — a real ccs fork stays open (window leg: live ccs->claude fork held open, PIDs captured ≥90s, refuting the earlier instant-close; exact launch command; env replicated — personal profile/cwd/CLAUDE_CONFIG_DIR; launcher tmpfile self-deleted). Tmux leg: cc-branch's tmux window ran the same launcher spawning the same live fork, launch+env replication identical (`ps -wwE` confirmed). The earlier slow-child-boot symptom traced to THIS session's 200+ turn parent transcript weight, not the port (normal parents boot fast) — the live forks were killed before the heavy boot to save tokens. ccs confirmed a real binary; the launcher tmpfile (`$TMPDIR`) is cross-process readable here — pinning it to `/tmp` is filed as a P3 hardening candidate, not a P2 fix. Child-boots-and-joins (the last smoke-gate leg, "(d)") completed via Carlos's own manual `cbus-go branch` runs (normal-transcript context, both window and tmux) directly confirming BOTH parent and child appear in `cbus list` — the child boots, joins, and registers. Recommendation upgraded to UNCONDITIONAL GO. WHOLE-PORT INVARIANT: across all 48 commits of the port so far (4cac62f..2aadf1a), the relay was never behaviorally modified — only refactored (shared framer extraction, `core.MaxMessageBytes` single-sourcing) with byte-parity proven at every step; `reframe_test.go` remains byte-unchanged from the pre-port audit throughout. Commits: 2b49b37, 2826de5, 638c20b, 2aadf1a. P2.6 CLOSED — PHASE 2 CLOSED, 6/6.

[2026-07-13 19:46:44 UTC] [Port/Go] Phase 2 (5/N) — harness layer + real flag parser (P2.5), CLOSED. Four components: `hook-exit` (runHookExit reads {session_id} off stdin w/ env fallback, leaves LOCAL registrations only w/ presence 'left' per channel, ALWAYS exits 0 — a hook must never fail the session; remote markers survive, since the relay has no leave endpoint and they die only via the ownerPid sweep). `bootstrap` (BootstrapPrompt ships the canonical fork-child prompt in the binary, extracted BYTE-EXACT from bin/cbus:832 w/ provenance in a comment; $ch/$parent substitution via strings.NewReplacer). `branch` (absorbs cc-branch.sh natively behind a pluggable TerminalForker; env replication — PATH/CLAUDE_CONFIG_DIR/cwd — modeled as a ForkSpec, testable via a fake forker; tmux unchanged, execs through /bin/sh which honors quoting). Real flag parser (`splitVerbArgs` replaces ad-hoc per-verb loops for send; two ruled deltas over bash: a `--` terminator ends option parsing, and `noExtra` makes trailing junk an error on fixed-arity verbs where bash silently discarded it; auth-set keeps its ordered flag loop — ordered/repeatable flags w/ stdin side effects don't fit a map parser). No cutover wiring yet (P2.6, user-gated). FINDING SAGA (F1, now fixed): the initial commit (f393158) deleted cc-branch.sh's mktemp self-deleting-launcher shim, building the iTerm2 osascript command with native Go quoting instead — following this project's OWN port-map.md §4 item 12, which wrongly characterized the shim as bash-only cruft. Reviewer's live iTerm2 probe (twice) proved iTerm2's AppleScript `command` param is tokenized BY ITERM2 ITSELF and does not honor POSIX quoting — the quoted one-liner mis-tokenized and launched nothing. FIX (d114688): resurrect the shim — `osaForkITerm` writes a 0700 self-deleting launcher script (env exports + cd + rm-self + exec, POSIX-quoted, the dialect layer iTerm2's tokenizer bypasses) and hands iTerm2 a BARE `/bin/bash <tmpfile>` command; `launcherScript` is a pure (spec, scriptPath) function, byte-for-byte testable; a load-bearing comment records the TRUE rationale + probe reference so it can't be "cleaned up" again. (The port-map.md mischaracterization that caused this had already been corrected in the docs, commit 618d171, before this fix landed.) Tests: launcherScript byte-exact, iterm2Command bare shape, TestLauncherScriptExecutes (runs the script via /bin/bash, proves PATH/CLAUDE_CONFIG_DIR/cwd replication + self-delete + exec end-to-end). Reviewer re-verified against REAL iTerm2 with a nasty-spec probe: byte-exact script content, self-delete confirmed. Differential vs bash: bootstrap byte-identical (4 cases + trailing newline); hook-exit rc 0 + empty stdout + local-leave + remote-marker survival. `go test -race -count=1 ./...` green both commits. Commits f393158 + d114688. P2.5 CLOSED — Phase 2 now 5/6.

[2026-07-13 19:12:32 UTC] [Port/Go] P2.4-F1 FIX CONFIRMED — P2.4 CLOSED FULLY (Phase 2 now 4/6 milestones). New `compatInboxPath(dir,ch,al)` does bash-verbatim concatenation (no `filepath.Clean`), used on exactly the two cross-process compat surfaces: the exec `--inbox` argv (via `InboxPath`) and Go's `argvContains` needle (new `metaInboxNeedle`, rebuilt from the raw CBUS_DIR + the peer's subpath so the spelling survives; handles legacy v1 + v2). File I/O keeps clean paths (the fd doesn't care; the kernel collapses `//` anyway). Tests: the existing byte-equal pin + a new non-clean-spelling case (CBUS_DIR trailing slash) asserting argv == needle == bash-verbatim, plus the ruled `TestFollowDirDeletionNeverExits` (whole alias-dir `rm` -> follower keeps polling -> reopens on recreate). LIVE: all four bash<->Go liveness directions read alive under a trailing-slash CBUS_DIR. `go test -race -count=1 ./...` green. Commit bc1940f.

[2026-07-13 19:05:02 UTC] [Port/Go] P2.4 VERDICT ADDENDUM (a7d2a4f, entry stands): APPROVED with one confirmed finding. Additional verification: a 1574-input fuzz pass over the framer/path-handling surface, green apart from the finding below. FINDING: path-spelling compat — Go's `filepath.Join` cleans a path (collapsing double slashes, trailing slashes, `.` segments) where bash's `inbox_path()` does raw string concatenation and preserves whatever spelling `$CBUS_DIR` was given; under a non-default CBUS_DIR spelling, the two followers' recorded/searched argv paths could diverge byte-for-byte, breaking bash↔Go cross-liveness even though same-spelling coexistence (today's default) is unaffected. This is the pre-registered "row-19 class" of port regression (port-map.md §3 row 19: "the subtlest port regression: everything demos fine single-session, then every armed listener reads off") — caught here specifically because the reviewer probed with a non-default CBUS_DIR spelling rather than the default. RULING: a `compatInboxPath` helper reproduces bash's raw-concatenation spelling verbatim, used only on the two cross-process compat surfaces (the follower's `--inbox` argv value and the liveness needle `argvContains` searches for) — normal internal file I/O keeps using the clean path. Rides a post-verdict commit together with a whole-dir (non-default-spelling) regression test.

[2026-07-13 18:53:12 UTC] [Port/Go] Phase 2 (4/N) — in-process blocking follower (local `cbus tail`). New `core.LocalEmit`: the LOCAL follower's framer, the counterpart to `Reframe` (relay). Two byte-visible differences on the parity domain — it KEEPS `kind=` in the header (presence renders locally; Reframe drops it) and appends the single `\n` stdout terminator that is part of every local write — so the golden contract holds: bash emit() == LocalEmit(line) == Reframe(line)+`\n`. Degenerates follow ruling D6, NOT bash-verbatim: non-JSON/non-string-field/empty/null/missing text pass THROUGH (fixing bash's None-leak), and a framed line missing from/to renders `?` to keep the local column — pinned in a divergence matrix citing D6, never in a golden corpus. `LocalEmit`/`Reframe` share `frameHead`/`frameBody`/`wrapBytes` (no second wrap copy); `Reframe` byte-untouched, its m3 matrix + golden stay green unedited. Presence(kind) golden `corpus_local.golden` generated by the same lifted `emit_golden.py`. New `internal/client/follow.go`: `ArmLocalTail` resolves the target (bare alias → own channel), requires the inbox to exist (join-first; meta not required), records `listenerPid=os.Getpid()` + `ownerPid` + refreshes `lastActivity` (D3) into meta (best-effort temp+rename, all other fields verbatim), then a TRUE `syscall.Exec` image-replaces into the follower — same pid, so the recorded pid IS the follower (Decision 2). The re-exec argv carries `--inbox <path>` byte-equal to bash `inbox_path()` (no EvalSymlinks/Abs) so bash-era `meta_listener_alive` (argv grep) reads this Go follower alive during coexistence; `os.Environ()` passed explicitly; `// COMPAT`-tagged (dies with P3 structural liveness). Follower loop (0.2s poll, `pend`-to-newline, dev+ino+size-regression rotation triple → reopen-from-0, one write+flush per frame, never-self-exit) with the ruled vanish-between-stat-and-open fix: reopen retries until success. Replay tri-state keyed off the meta's listenerPid (absent/null → first-arm replay from byte 0 `+1`; recorded int → re-arm seek EOF `0`). No daemons — a plain foreground child that dies with the Monitor. LIVE differential vs bash `cbus tail` (hermetic CBUS_DIR, bounded capture): BYTE-IDENTICAL across replay/append/truncate/rm-recreate incl. a `kind=presence` line (2112 B, 9 frames); same-pid across the exec confirmed (meta.listenerPid == launched pid); os.Environ passthrough (CBUS_DIR + sentinel in the follower's env); bash↔Go cross-liveness (each reads the OTHER's follower as alive). Both platforms green under `-race -count=1`. Commit a7d2a4f.

[2026-07-13 18:15:17 UTC] [Port/Go] Relay adopts core.MaxMessageBytes (closes P2.3 anti-drift rider); coder session handoff. `relay/cmd/cbus-relay/main.go`'s handleSend hardcoded `http.MaxBytesReader(1<<20)`; now adopts `core.MaxMessageBytes` so the local-send and relay-body caps cannot silently drift apart (one-directional anti-drift, following the m5 single-sourcing precedent). Pure refactor — same 1 MiB value; conformance rig re-run green, reframe_test.go untouched. Commit 82fd4ac. SESSION HANDOFF: coder hit a context-limit checkpoint after 21 commits; the P2.4 spec (incl. the LocalEmit-keeps-kind note) is preserved in project memory for a successor session to pick up P2.4.

[2026-07-13 18:10:49 UTC] [Port/Go] Phase 2 (2/N) — PeerStore mutations + presence (P2.2), CLOSED after a concurrent-join fix. Local transport mutations with the atomicity idioms mapped 1:1: `store.go` Join (bare-mkdir EEXIST claim with unified pick+claim, inbox truncate-at-join, meta temp+rename, dual-write lastActivity per D3), Leave, Rename (same-parent mv + meta.alias rewrite preserving raw pids; alias always a string, dropping jset's digit-coercion — a ruled C-delta), Unregister, PruneChannel (dot-prefixed same-parent rename reap + 3-way re-verify + rmdir), PruneRemoteMarkers, Prune, LeaveRemote. `presence.go` BroadcastPresence (join/leave/rename/departed) with the !PeerDead recipient filter, from-subject vs skip-actor split, once-only departed via the reap mv-claim; O_APPEND single-write appends. Presence lines are canonical-Go (D8) — the follower frames them identically to bash's, so delivered events are byte-identical even though the raw line is compact (the D8-extension-to-presence ruling). cmd/cbus wires join/register/leave/rename/unregister/prune. Live differential vs bash cbus on a shared $CBUS_DIR: meta.json byte-identical modulo the D3 lastActivity line; bash reads go metas; bash presence lands in go peers; go prune reaps a bash-written dead peer. FINDING (F1, now fixed): the auto-pick claim loop burned all 50 retries inside a sibling's mkdir->meta window — bash's ~ms subshell pick was an accidental backoff the Go µs loop didn't have; reviewer repro'd 8 concurrent joins losing 3 ("cannot claim an alias"). Fix: claimAlias remembers EEXIST-failed aliases per invocation and excludes them from later picks (only ErrExist poisons the exclude-set), converging in one extra round with no sleeps — deterministic, Class-B contract (a unique alias per joiner; exact fork-N numbering under concurrency was never deterministic and need not match bash). Regression: 24 concurrent claimAlias calls under -race -> 24 unique aliases; mixed 4-go+4-bash concurrent-join repro -> 8/8 unique (was 5/8) on both rounds. Resent micro-notes from review: cwd() uses os.Getwd() (physical path) where bash's $PWD is logical (symlink-preserving) — a cosmetic display-field divergence, not a correctness issue; setMetaAlias is a best-effort no-op parity match for bash's rename `|| true`. Commits c3db6b5 + 0e88056. CONFIRMED CLOSED — reviewer's mixed 4+4 harness 8/8 both rounds.

[2026-07-13 17:39:14 UTC] [Port/Go] Phase 2 (3/N) — local send + send-gate + MaxMessageBytes. New `core.MaxMessageBytes = 1 MiB`, aligned with the relay's POST /send body cap so both paths share one message-size invariant. New `internal/client/send.go` LocalSend: the send-gate trichotomy (never-armed accepted / live accepted / armed-dead refused unless --force, then queued best-effort with a warn), the local from-chain (own reg in the target channel -> first reg anywhere -> $CBUS_ALIAS -> <shorthost>-<ppid>), O_APPEND single-write append, and a REJECT (never truncate) past MaxMessageBytes — a deliberate delta from bash's unguarded stdio cliff. `cmd/cbus` wires the local send branch with the --from "" null-check. Presence cross-parse rider (P2.2 flag ruling): a go-canonical {from,to,ts,kind,event,text} presence line and its python-marshaled equivalent frame BYTE-IDENTICALLY via the lifted emit() including the kind= header — the D8 extension to presence proven empirically, recorded in golden_test's header; reviewer re-verified the cross-parse using the real P2.2 rename artifact rather than a synthetic one. Tests: send-gate (never-armed/live/dead+force), from-chain + unroutable fallback, max-line reject-not-truncate. Differential vs bash cbus: send stdout byte-identical; go+bash send lines coexist in a shared inbox and decode identically. Both platforms green. Approved. One anti-drift rider — the relay adopts core.MaxMessageBytes too — rides the next commit.

[2026-07-13 02:43:35 UTC] [Port/Go] CORRECTION to the P2.1-F1 entries (dd86250, effe3e4): the stated mechanism — "darwin KERN_PROCARGS2 serves cached argv for a zombie process" — is RETRACTED after a conclusive reviewer re-run. The original repro caught the process in a still-running window, not a true zombie; re-verified, a genuine darwin zombie's KERN_PROCARGS2 call fails naturally with EINVAL, so the pre-fix code was likely already outcome-correct (argv-dead) without the SZOMB guard. The fix itself (explicit SZOMB detection) and its regression test (TestArgvClauseZombieDead) are RETAINED as a fail-open hedge — harmless and arguably clearer than relying on an unstated kernel behavior. The now-inaccurate code comments (procinfo_darwin.go, liveness.go) describing the retracted mechanism are queued for a reword at P2.2 open.

[2026-07-13 02:40:05 UTC] [Port/Go] Phase 2 (1/N F1 fix) — darwin zombie reads argv-dead. Closes P2.1's confirmed finding: unlike linux (/proc/<pid>/cmdline goes empty for a zombie) and unlike `ps` ("<defunct>"), darwin's KERN_PROCARGS2 kept returning a zombie's cached argv with err=nil, so a zombie follower read ALIVE to Go while bash read it dead. procArgs now detects SZOMB first (pbi_status at offset 4 of the proc_bsdinfo buffer procParent already fetches) and returns ESRCH, so the argv clause reads dead, matching bash. Corrected the prior false comment claiming KERN_PROCARGS2 errors for zombies. New regression TestArgvClauseZombieDead per reviewer's repro (exec("true").Start() unwaited -> zombie; kill -0 succeeds yet argv clause reads dead). Passes on darwin (MBP) + linux (NUC). P2.1 finding closed.

[2026-07-13 02:30:12 UTC] [Port/Go] Phase 2 (1/N) — liveness off ps-spawns (sysctl/procfs) + peer_dead. Moved the three-clause liveness predicate + find_owner_pid off ps-spawns: darwin reads argv via sysctl KERN_PROCARGS2 and ppid/comm via proc_pidinfo(PROC_PIDTBSDINFO); linux via /proc/<pid>/cmdline + /proc/<pid>/stat. argvContains returns false on any read error (ESRCH/EPERM/zombie) so the argv clause reads DEAD with no invented leniency. pidAlive stays kill -0. PeerDead: never-armed 10-min grace preferring a dual-written lastActivity field, else meta mtime (fallback drops in P3); armed -> !alive. Removed the ps-shelling helpers. Validated on BOTH platforms: self-parse procArgs(self)==os.Args (MBP darwin + NUC linux); real-process liveness tests run through the sysctl/procfs path; dead/nonexistent pid -> error -> dead. CONFIRMED FINDING (reviewer-reproduced empirically): darwin zombie liveness inversion — KERN_PROCARGS2 serves cached argv for a zombie process, so a zombie would read alive vs bash's ps-based defunct-reads-dead; fix cluster binds at P2.2 open. Offsets otherwise statically verified; live differential byte-identical. CONDITIONAL APPROVE on that finding.

[2026-07-13 02:06:41 UTC] [Port/Go] Phase 1 complete: cbus-go remote+read-only client side-by-side, 6 commits, all approved, installed ~/.local/bin/cbus-go. Phase 1 (6/N) — read-only local verbs (whoami/inbox/channels/list). New `internal/client/liveness.go`: MetaListenerAlive three-clause predicate (listenerPid alive + argv references the inbox via ps -ww + ownerPid alive if recorded), faithful to meta_listener_alive; ReadPeerMeta torn-read tolerant. identity.go: SessionMarkers for whoami's remote-marker line class. `cmd/cbus whoami` (local regs + remote markers, exit 1 when neither exists), `inbox` (path, no trailing newline, bare-alias refused), `channels` (peer/listening counts), read-only `list` (pid/host/cwd + --active + legacy-v1 line); active/peers aliases wired. Tests: MetaListenerAlive against real processes (tail -f w/ inbox in argv = listen; null/dead/argv-mismatch/dead-owner = off). MBP seeded differential byte-identical across whoami/inbox/channels/list/list-dev/list--active. NUC LIVE local-mode differential byte-identical (cross-compiled temp binary, run then deleted, no install) — reviewer did not reproduce this leg himself (crosses a permission boundary he doesn't have, no NUC shell access; correct/expected, not a gap), accepted coder's session transcript as the evidence of record. A1/A3/A5/A6 contract-class evidence bundle now complete. Approved — PHASE 1 CLOSED six-for-six (40c82f0..b474c0d).

[2026-07-13 01:57:22 UTC] [Port/Go] Phase 1 (5/N) — remote tail arm-spec (A5) + .remote identity markers (A3); --from "" now dies. New `internal/client/marker.go`: WriteRemoteMarker writes this session's `.remote/<host>/<channel>/<sessionId>` = {alias,ownerPid,ts} pretty-printed (json.MarshalIndent == python's json.dump(indent=2), byte-identical), replacing a legacy machine-global FILE marker first; OwnerPID walks PPID up to the claude ancestor (find_owner_pid parity via ps) with a PPID fallback; Now() = date -u second precision. `remote.go` RemoteTailSpec builds the ws arm-spec byte-identical to cmd_tail_remote's heredoc (ws-scheme swap, subprotocol token, em-dash). `cmd/cbus tail <ch>@<host>/<al>` prints the spec + writes the marker (print-only, token-only — local tail stays Phase 2). Ruled fix from P1.4: `send --from ""` now dies (bash's `${2:?}` null-check parity) instead of silently falling back to the default. Tests: marker indent=2 golden, write/read round-trip, legacy-file replace, unroutable fallback, arm-spec format, --from "" die. LIVE differentials vs bash cbus: A5 arm-spec byte-identical; A3 proven both directions — bash sends via a Go-written marker AND Go sends via a bash-written marker, marker format Go==python identical. Local marker test residue cleaned; only relay spool residue remains (no GC). Approved clean — reviewer reproduced all evidence live, including the end-to-end deep path.

[2026-07-13 01:48:02 UTC] [Port/Go] Phase 1 (4/N) — remote send/list client (M7), explicit timeouts, no retry. New `internal/client/remote.go`: in-process net/http with explicit timeouts (4s connect, 20s total), NO retry (POST /send is non-idempotent and unkeyed); ResolveRemote builds the front door + auth headers (always Bearer; CF-Access pair public-mode-only, local skips it); missing creds are hard errors pointing at `cbus auth set`. RemoteSend (POST /send) and RemoteList (GET /peers). identity.go: RemoteFromDefault (session marker -> <shorthost>-<ppid> chain, deliberately never falling back to local registrations — the by-design remote/local asymmetry), ShortHostname. `cmd/cbus send <ch@host/al> [--from|--force] TEXT` (--force ignored remotely) and `list [<ch>]@<host>` (sorted, listen/off + queued + lastSeen, RFC3339Nano round-trip); `active` reproduces the remote dead-quirk. Tests: hermetic httptest (public sends CF, local skips CF, missing-creds errors, /peers decode) via CBUS_SITE_<TESTHOST>_URL, never binding :8090; reviewer's double-'-' drain-once rider pinned. FIRST LIVE public-mode differential vs the real nuc relay: `list p1diff@nuc` and `send --from` both byte-identical to bash cbus (throwaway p1diff channel residue left on the relay — no GC exists). Incidental A6 proof: real keychain creds read successfully. Approved clean — reviewer re-ran the live differential, byte-identical. One ruled fix (explicit empty --from must die, not silently fall back) rides P1.5.

[2026-07-13 01:37:47 UTC] [Port/Go] Phase 1 (3/N) — auth set/status credential store (M8). New `internal/client/cred.go`: CredStore over a platform backend — macOS security(1) (service cbus-relay-<host>, account=field; secret rides `security -i` stdin, never argv) or Linux XDG 0600 files (dir 0700, no trailing newline); keeps shelling to security(1) (no in-process Keychain, no ACL prompt); the security runner is injectable so unit tests assert exact argv/stdin without executing it. `cmd/cbus auth set <host> [--token|--cf-id|--cf-secret V]` (V='-' drains stdin once, all whitespace stripped) and `auth status [host]` (masked last-4; validates the host, closing the bash auth-status gap as a documented C-delta). Tests: mock-runner argv/stdin assertions, file-backend perms/round-trip in a tempdir, StripWhitespace/MaskTail, handlers via a file store, plus an OPT-IN (CBUS_KEYCHAIN_IT) temp-keychain integration test (explicit keychain path every call, never login keychain/search list, delete-on-cleanup) — ran once: PASS, search list unchanged, no leak. Approved clean — reviewer re-ran the keychain integration himself, leak checks negative.

[2026-07-13 01:27:53 UTC] [Port/Go] Phase 1 (2/N) — address resolution (M2) + bare-alias resolution; probe no-redirect fix. New `internal/client/addr.go`: IsRemote (@ anywhere = remote), ParseLocal (split at first /, empty-channel-skips-validation quirk kept, a/b/c -> bad alias), ParseRemote (first @ then first /, empty parts skipped), Parse wiring bare-alias resolution via an injected Resolver; bad names are hard errors per the approved soft->hard ruling, reusing core.ValidName. New `internal/client/identity.go`: CBUSDir, SessionID, ResolveSelf ($CBUS_DIR/*/*/meta.json scan by sessionId, dot-dir blind, glob order faithful to find_peer_channel), FindPeerChannel, torn-meta tolerant. Table tests from protocol §1.2 + identity tests over a seeded temp $CBUS_DIR. P1.1's probe redirect-following finding fixed: probeLocalOK sets CheckRedirect -> ErrUseLastResponse so the loopback trust-by-port probe never follows a 3xx off-loopback; new httptest 302 case asserts it selects public. Approved clean — reviewer mutation-verified the redirect guard.

[2026-07-13 01:21:30 UTC] [Port/Go] Phase 1 (1/N) — cbus-go skeleton + front-door/endpoint resolution; conformance rig hardened. New `cmd/cbus` (installs as cbus-go): verb-dispatch skeleton, --help, not-implemented stubs, unknown verb -> exit 1. New `internal/client/endpoint.go`: SiteURL (nuc built-in + CBUS_SITE_<HOST>_URL mangling, strip-one-trailing-underscore quirk kept; unknown host now a hard typed error per the P1 soft->hard ruling), ResolveFrontDoor (0.3s exact-'ok' probe), WSURL scheme swap; tested via httptest, no real :8090 bind. Conformance rig hardened per m5's F1: trackSources ties go test's cache to the runtime-built relay (verified empirically) + 401/400 negative assertions + doc.go gotcha note. Standing gofmt gate caught and fixed its first real slip. One finding (probe redirect-following vs curl parity) rides P1.2.

[2026-07-13 01:05:20 UTC] [Port/Go] Phase 0 complete: shared core + conformance harness, 7 code commits, all reviewer-approved, HEAD 253f4a2. Phase 0 (5/N) — wire conformance rig vs the real relay binary + m4 riders + relay wire-struct adoption. New hermetic `relay/internal/conformance` rig builds/runs the real cbus-relay binary (temp spool, ephemeral port, env token) and drives POST /send, GET /peers, GET /tail(ws) against core.SendReq/PeersResponse/Message — proves the shared wire structs match the live relay end-to-end, /tail delivering exactly `core.Reframe` of the stored line. m4 riders: emit_golden.py now byte-exact vs bin/cbus:517-551 (◀ U+25C0 escapes restored, sole declared omission = argv line 521), stdin UTF-8 reconfigure is driver-only, spoof case relabeled. relay/cmd/cbus-relay/main.go adopts core.SendReq/PeersEntry (pure refactor, byte-identical wire). Reviewer ran the rig himself (-count=1, green 1.1s), reframe_test.go byte-diff EMPTY across the whole phase (5e71ddc..253f4a2). Carry-forwards to P1: rig test-cache staleness (-count=1), rig negative assertions, deploy.sh NUC cleanup check.

[2026-07-13 00:51:45 UTC] [Port/Go] Phase 0 (4/N) — golden framer parity corpus + oversize flip-point + gofmt gate. Added `internal/core/testdata/{emit_golden.py,gen_corpus.py,corpus.jsonl,corpus.golden}` (a verbatim lift of the bash follower's wrap()/emit(), bin/cbus:515-551) and `golden_test.go`'s `TestGoldenCorpusParity` pinning `core.Reframe` byte-for-byte to the bash emitter on plain tool-authored shapes; one-time header-recorded validations: lift == live hermetic follower capture, and D8's cross-parse assertion (Go-marshaled vs python-marshaled) both proven byte-identical. `TestReframeOversizeFlipPoint` pins the §4.4 header-less-total warn threshold (closes m3's F2); `gofmt -w` + new standing `TestGofmtClean` gate (closes m3's F1); D8 comment fixes in message.go/message_test.go. Reviewer independently reproduced the live capture and cross-parse byte-identically; one provenance rider (byte-exact ◀ U+25C0 restoration + a declared argv-line omission in the generator's BEGIN marker) plus two nits ride m5.

[2026-07-13 00:37:30 UTC] [Port/Go] Phase 0 (3/N) — degenerate-input matrix + rune-safe 440B wrap property tests. New `internal/core/frame_test.go` pins the relay-side framer divergence matrix (protocol.md §4.5, 8 cases) against `core.Reframe`, plus property tests for the rune-safe 440-byte wrap across 1/3/4-byte runes x lengths 0-300 x limits {1,3,4,7,440} and a full-framer rune-safety check. Test-only, no production changes. Conditionally approved: findings F1 (gofmt) and F2 (oversize flip-point pin) folded into m4, closed there.

[2026-07-13 00:33:32 UTC] [Port/Go] Phase 0 (2/N) — internal/core domain types + single-sourced name validation. Added `core.Message{from,to,ts,text,kind,event}` with key-order-agnostic lenient decode (a flexString shadow accepts JSON numbers/null where strings are expected — the "int alias" tolerance, applied to typed fields since Decoder.UseNumber can't reach them) and a Marshal that emits the plain-line shape {from,to,ts,text} (kind/event omitted unless presence). Added wire structs `core.SendReq` (POST /send body) and `core.PeersEntry`/`PeersResponse` (GET /peers). Moved `ValidName` into core and had the relay adopt it (dropped its private nameRe/validName + the now-unused regexp import) so channel/alias/host validation is single-sourced and parity is compile-enforced. Unit tests for the §1.1 name property table, lenient-decode matrix, marshal shapes, and /peers decode incl. the zero-time sentinel. Folded reviewer class-C notes: deploy.sh now prunes the pre-restructure cmd/ + service leftovers that rsync --delete's scope leaves on the NUC; frame.go constant anchors qualified as pre-extraction.

[2026-07-13 00:23:31 UTC] [Port/Go] Phase 0 start — promoted the Go module to the repo root (`module claudebus`, was `claudebus/relay`) and added `internal/core` as the shared package both the relay and the future Go client import. Moved the message framer (reframe + wrapBytes + wsFrameSafe, main.go:202-252) verbatim into `internal/core/frame.go`; the relay keeps a thin `reframe()` = `core.Reframe` wrapper so the delivery loop and reframe_test.go are byte-unchanged (pure refactor; relay tests green). Framer output carries no trailing newline (the ws OpText frame); the local follower owns the single '\n' stdout terminator. Measured Monitor constants (500/440/2800) centralized with provenance re-measured live 2026-07-13 — harness now emits an explicit '...(truncated)' marker at both the per-line cap and the ~3000 notification ceiling. deploy.sh retargeted for the root-module layout and validated by local rsync-simulation (native + linux/amd64 cross-compile OK). Installed go1.26.5 locally (MBP had no toolchain; relay was always built on the NUC).

[2026-07-12 22:14:17 UTC] [Docs] Full behavioral audit at HEAD f213e26 (31-agent workflow: 5 subsystem mappers, 3 critics x2 rounds filling 56 coverage gaps, 2 port analysts + synthesis). New human docs under docs/architecture/: overview.md (system + topology + security model + design rationale), command-reference.md (every subcommand/flag/env/exit code/error string), protocol.md (on-disk formats, framing grammar, relay HTTP/WS API, Maildir spool — the port compatibility contract), port-map.md (module decomposition M1-M12, unix-primitive inventory, contract classes A/B/C, phased bash→Go migration plan). Canonical LLM docs mirrored to ~/dev-docs/projects/claudebus/ (index, architecture, behavior-spec, port-map). Docs describe as-is behavior incl. quirks; shipped-README drift is registered, not inherited.

[2026-07-12 20:48:19 UTC] [CLI] New `cbus hook-exit` for a Claude Code SessionEnd hook: reads {session_id} from the hook's stdin JSON (the hook env may not export CLAUDE_CODE_SESSION_ID), falls back to the env var, and announces departure (cmd_leave -> presence 'left' per local channel). Makes clean exits notify peers IMMEDIATELY instead of waiting for the lazy prune 'departed'. Best-effort (subshell so cmd_leave's not-joined exit can't fail the hook); only graceful exits fire SessionEnd, so the prune backstop still covers hard kills. Wire it via ~/.claude/settings.json SessionEnd -> `cbus hook-exit`.

[2026-07-12 20:41:06 UTC] [Docs/CLI] Fix a friction point: sessions kept running `cbus tail` in Bash (where the follower never exits and blocks forever) instead of arming it via the Monitor tool. Strengthened every arm/re-arm hint the model reads — cmd_join/rename/register printfs, the fork bootstrap prompt, the usage text, and the /bus-join, /bus-branch, /bus-rename skills — to state explicitly: `cbus tail` is the Monitor tool's source, NEVER a Bash command; a bash call blocks forever.

[2026-07-12 20:20:38 UTC] [Core] Presence hardening from a second Fable-5 diff review: (1) reap temp is now DOT-prefixed (.reap.$$.<peer>) so the */ glob never rescans a half-reaped dir and a crash-orphaned temp is inert, not a phantom peer; (2) prune re-verifies !peer_dead AFTER the mv-claim and restores the dir if a fresh join reclaimed the alias mid-mv (was: silent unregister + false departed); (3) broadcast_presence split from-subject vs skip-actor so rename-reclaim (from=new, skip=old) no longer self-echoes to the renamer's stale tail; (4) inbox append guarded (2>/dev/null||continue) so a vanished target under set -e can't abort a broadcast (in cmd_leave that would've left the session registered); (5) one now() per event, not per recipient; comment de-overclaimed. Pre-existing rename mv→re-arm message-loss window filed as cbus-8no.

[2026-07-12 20:04:18 UTC] [Core] Presence announcements (cbus-2r7, local): join/leave/rename now broadcast a kind=presence event to every non-dead channel peer, and every removal path (prune_channel, cmd_unregister, cmd_rename's dead-name reclaim) broadcasts departed. New broadcast_presence() helper uses the SAME !peer_dead rule as send (so a joined-but-unarmed peer still gets it, replayed on first arm — not meta_listener_alive, which would skip it). prune's departed is guarded by an atomic mv-claim so two concurrent reapers announce once. cmd_bootstrap drops its manual "joined as X" announce (join now auto-broadcasts — no double event). App-agnostic (no tmux/iTerm2). Remote (@host) presence is a filed follow-up (cbus-ijx.5 — relay strips kind today). Reviewer-vetted (dev@nuc/reviewer, Fable 5): all 10 findings folded into the plan/impl.

[2026-07-12 05:51:32 UTC] [Relay] Remote (@host) ws-tail delivery now reframes server-side, at parity with the local tail fix — long cross-machine messages arrive whole instead of truncating at 500. cbus-relay `handleTail` rewrites each stored {from,to,ts,text} into the same `◀ cbus msg`/wrapped-body/`◀ cbus end` block and sends it as ONE multiline ws frame. Measured the harness Monitor's ws behavior first (throwaway local ws server): a multiline frame is truncated PER-LINE at 500 (not 500 total), but there is ALSO a ~3000-char per-notification ceiling shared by BOTH paths — so >~2800-char messages still truncate; the reframed header now carries a `⚠truncated~<N>B` notice instead of cutting silently. Also fixed relay/deploy.sh: it used `systemctl enable --now` (a no-op on an already-active unit) so every prior deploy rebuilt the binary but kept the old process — now `enable` + `restart`. Go unit tests for reframe/wrap added; deployed to NUC + verified live NUC->MBP. Follow-up cbus-mjz closed; shared >2800 ceiling tracked separately.

[2026-07-12 01:40:50 UTC] [Core] `cbus tail` reframes messages so long ones survive the harness Monitor's 500-char-per-line truncation and arrive whole in the FIRST event (kills the "truncated again, let me read the full message" double roundtrip). Replaces `exec tail -F` with an `exec`'d python follower: same single-pid liveness model (argv carries the inbox path for meta_listener_alive; no child tail to orphan), replay-from-start on first arm / from-end on re-arm, inode+shrink reopen for rejoin/rename. Each message renders as `◀ cbus msg from=… to=… ts=… [kind=…]` + text soft-wrapped at 440 bytes + `◀ cbus end from=…`; the Monitor batches the short lines into one notification. UTF-8-safe under any locale (stdout reconfigure + ASCII `◀` in source). Local file-bus only — remote @host ws frames are unchanged. Docs (bus-join/README/CHEATSHEET) updated.

[2026-07-08 21:30:00 UTC] [Core] Session-scoped bridge identity (fixes cross-session marker leak + impersonation): markers move to .remote/<host>/<channel>/<sessionId> = {alias, ownerPid, ts}. whoami/send/leave operate on the caller's marker only — sessions never inherit each other's aliases; whoami wording is now "remote from-default", not reachability (cbus list @host is the truth source). prune sweeps dead-owner + legacy machine-global markers; legacy file->dir migration on next arm. Client-only; relay untouched.

[2026-07-08 19:45:00 UTC] [Core] Review-hygiene fix set: relay credentials no longer appear in any process argv (curl auth via config-on-stdin `-K -`; Keychain writes via `security -i`); the eval in header assembly is gone with the refactor; CBUS_SITE_<HOST>_URL var name now sanitizes non-alphanumerics so dotted hosts resolve under set -eu.

[2026-07-08 17:09:00 UTC] [CLI/Commands] New `cbus rename <new-alias> [channel]` — true local rename: mv the peer dir + rewrite meta.alias so multiple forks are legible in `cbus list` (fork-3 → discovery). Local-only (refuses @host remote aliases — relay-side); resolves this session's registration, demands a channel when joined to >1, refuses names held by a live listener, reclaims dead ones, preserves the inbox. The live tail is stale after the mv, so it prints a re-arm reminder. New `/bus-rename` slash command runs it then TaskStops + re-arms the Monitor on the new address. (CC TUI session title stays manual: `/rename` — not externally settable in v2.1.204.)

[2026-07-08 17:05:00 UTC] [Core] Remote channels: <channel>@<host>/<alias> addressing in send/tail/list/leave (relay UNTOUCHED — reuses .1 contract). cbus auth set/status (Keychain on macOS, 0600 files on Linux; no tokens in code), healthz-probe front-door autodetect (loopback on relay host, else CF front door), remote tail emits the Monitor {ws:} arm spec + records an identity marker for routable from. Verified live: queued-replay + live push into a real Monitor via the relay.

[2026-07-07 17:15:00 UTC] [Docs] New docs/prior-art-and-cc-internals.md — durable record of the tool landscape review (session-bridge, AMQ, principle-19, cyrusagents inbox, mcp_agent_mail, CCS, claude-intercom), the Claude Code internals probes (why native SendMessage can't cross sessions), the constraints that shaped the design (Monitor ws: headers, safety classifier, fork transcript semantics, push delivery), and the full decision-log timeline with shas. bdx cbus-foc/cbus-oq9 epic context appended with pointers.

[2026-07-07 16:00:00 UTC] [Relay] New relay/ — std-lib-only Go daemon on the NUC: POST /send (bearer) → Maildir spool → WS /tail (subprotocol token, k8s pattern) → Monitor {ws:} events. Presence heartbeat, single-tail displacement (dup-free, race-fixed after gpt-5.5 review), systemd unit, deploy.sh. Verified end-to-end incl. kill -9 durability and a real Mac Monitor consuming a relay push.

[2026-07-07 04:05:00 UTC] [CLI/Commands] New `cbus branch [target] [channel]` — join + channel-derive + fork with bootstrap prompt in one command. `/bus-branch` slims to two tool calls (branch, then arm Monitor); cc-branch.sh and its hardcoded path drop out of the skill's allowed-tools (helper resolved by cbus, override via CC_BRANCH).

[2026-07-07 03:40:00 UTC] [Docs] Corrected delivery semantics: Monitor events are push — an idle session wakes and acts autonomously (verified live: parent replied to a bus message with no user present); "turn boundary" only defers delivery on a busy session.

[2026-07-07 03:04:06 UTC] [Core] `send` now accepts a joined-but-never-armed peer (first arm replays the inbox); only a dead ex-listener is refused. Fixes the self-contradiction with the join→arm replay design.
[2026-07-07 03:04:06 UTC] [Core] Atomic alias claim on `join` — bare `mkdir` as the lock, so concurrent joins can't pick the same alias and truncate each other's inbox.
[2026-07-07 03:04:06 UTC] [Core] Re-arm no longer redelivers the whole inbox — `-n +1` only on first arm, `-n 0` after. `--force` to a dead ex-listener is now documented best-effort.
[2026-07-07 03:04:06 UTC] [Core] Reject `.`/`..` in channel/alias names everywhere (`valid_name` + `split_target`) — closes a self-inflicted `rm -rf` path escape.
[2026-07-07 03:04:06 UTC] [CLI] New `cbus bootstrap <channel> [parent]` prints the canonical fork-child prompt; `/bus-branch` consumes it so the prompt can't drift from CLI behavior.
[2026-07-07 03:04:06 UTC] [Commands] `/bus-branch` reverted to arm-before-fork: the "no completion record" note is unavoidable (child resumes the transcript at boot, after the parent armed) and the old ordering opened an announce race. Note documented as cosmetic; `/bus-listen` reply guidance now warns about unroutable `hostname-PID` senders.

[2026-07-07 00:20:44 UTC] [Core] Initial claudebus: file-based message bus between live Claude Code sessions, built on the Monitor tool + plain files.
[2026-07-07 00:20:44 UTC] [Core] Named channels — peers addressed as `channel/alias`; bare alias resolves within your own channel(s); `global` reserved as the machine-wide orchestrator bus.
[2026-07-07 00:20:44 UTC] [Core] Auto-recycled aliases — `cbus join` auto-picks `main`/`fork-N`, is idempotent, and prunes dead peers in the channel first (10-min grace for a joined-but-not-yet-armed peer).
[2026-07-07 00:20:44 UTC] [Liveness] Hardened listener liveness — trust a live pid only if its args reference the inbox (kills pid-recycling false-positives); record owning `claude` pid so a crash-orphaned tail reads `off`.
[2026-07-07 00:20:44 UTC] [CLI] Added `cbus active [channel]` / `cbus list --active` to list only currently-listening peers; `cbus channels` summarizes channels.
[2026-07-07 00:20:44 UTC] [Commands] `/bus-branch` forks before arming the parent's Monitor (no stale "no completion record" task in the child); `/bus-listen` + `/bus-branch` default the channel to the repo name.
